# スマホ1台で「実寸の点群」をつくる — 長さはどこから来るのか

写真から立体を起こす技術（フォトグラメトリ）には、昔から困ったところがあります。

**写真をどれだけ集めても、写ったものの「大きさ」は決まりません。**

形は出ます。比率も出ます。でも「この壁は何メートルか」には答えられない。
だから普通は、既知の長さのものを一緒に写します。定規、スケールバー、対空標識。

このプロジェクトは、それを置かずに実寸を出します。
**長さの単位は、スマホの中の加速度計が持ち込みます。**

作ったものは GitHub にあります（末尾に一覧）。この記事では、
なぜそれで実寸が出るのかという理屈と、実際どれくらい合っていたのかを書きます。

結論を先に書くと、こうなりました。

| | 実測 |
|---|---|
| **形と大きさ**（相対） | **1%以内**。173cmの巻尺を三角測量して172.6cm |
| **置き場所と向き**（絶対） | 水平2.4m / 方位4.7度 |

この2つを混ぜないことが、この記事でいちばん言いたいことです。

---

## 1. なぜ写真だけでは大きさが決まらないのか

### 1-1. カメラは何をしているか

まず、カメラが世界をどう平面に写しているかを式にします。

いちばん単純なモデルは**針穴写真機**です。箱の前面に小さな穴を開け、
奥の面にスクリーンを置く。ある一点から出た光のうち、穴を通った1本だけが
スクリーンに届くので、点が点として写ります。

穴を原点にして、カメラが向いている方向を Z 軸、右を X 軸、上を Y 軸とします。
スクリーンは穴から距離 `f` のところにあるとします。

空間の点 `(X, Y, Z)` から出た光が穴を通ってスクリーンに当たる位置を
`(x, y)` とすると、**相似な三角形**が2つできます。

```
        物体側              |  カメラ側
                            |
    (X, Y, Z) ●             |
              \             |
               \            |
                \           |
    ─────────────○──────────┼─────  ← 穴（原点）
                 \          |
                  \         |
                   ● (x, y) |  ← スクリーン（距離 f）
```

「高さ ÷ 奥行き」がどちらの三角形でも等しいので、

```
x / f = X / Z
y / f = Y / Z
```

つまり

```
x = f · X / Z
y = f · Y / Z
```

これが投影の式です。**奥行き Z で割る**、ここが本質です。
遠いものが小さく写るのは、この割り算のためです。

実際の画像は画素で数えるので、1mmあたり何画素かを掛け、
画像の中心のずれを足します。

```
u = fx · X / Z + cx
v = fy · Y / Z + cy
```

`fx`, `fy` を**焦点距離**（画素単位）、`cx`, `cy` を**画像中心**と呼びます。
このプロジェクトの端末では `fx ≈ 1388.6` でした。

### 1-2. 大きさが決まらない、を式で見る

さて、ここからが本題です。

空間にある点をぜんぶ `k` 倍に拡大して、**カメラの位置も同じ `k` 倍**に
動かしたとします。点は `(kX, kY, kZ)`、カメラは元の位置の `k` 倍。

このとき、投影された像はどうなるでしょうか。

```
u = fx · (kX) / (kZ) + cx = fx · X / Z + cx
```

**`k` が約分されて消えます。**

つまり、10cmの模型を10cm離れて撮った写真と、
10mの実物を10m離れて撮った写真は、**画素として完全に同じ**になります。
何枚撮っても、どの角度から撮っても、これは変わりません。

これが「写真からは大きさが決まらない」ということの正体です。
情報が足りないのではなく、**原理的に同じ像になってしまう**のです。

### 1-3. 2枚あれば形は出る

大きさは決まりませんが、**形**は2枚あれば出ます。

同じ点を2つの位置から撮ると、その点は画像上で少しずれた位置に写ります。
このずれを**視差**（parallax）と呼びます。

2台のカメラが距離 `B` だけ離れて同じ方向を向いているとき、
距離 `Z` にある点の視差 `d`（画素）は

```
d = fx · B / Z
```

これを `Z` について解くと

```
Z = fx · B / d
```

**視差が分かれば奥行きが出ます。** ただしこの式には `B`（カメラ間の距離）が
入っています。`B` が分からなければ `Z` も分からない。
`B` を2倍だと思えば `Z` も2倍になる。結局、大きさは `B` 次第です。

だから**どの分野も、既知の長さを1本どこかに入れています。**

| 分野 | 既知の長さ |
|---|---|
| 測量 | 基線（別途きちんと測る） |
| 写真測量 | スケールバー、対空標識 |
| ステレオカメラ | 2つのレンズの間隔（工場で固定） |
| LiDAR | 光速 × 往復時間 |
| **このプロジェクト** | **加速度計が積分した移動距離** |

---

## 2. 加速度計が長さを持ち込む — 物理

### 2-1. 加速度計は何を測っているか

加速度計の中身は、**ばねに吊るされた小さな錘**です
（実際はMEMSという微細加工の櫛歯構造ですが、原理は同じです）。

錘が加速されると、ばねが伸びます。伸びた量 `x` から力が分かります。

```
F = k · x        （ばねの力。k はばね定数）
a = F / m        （ニュートンの運動方程式。m は錘の質量）
```

まとめると

```
a = (k / m) · x
```

**ここが肝心です。** `k`（ばね定数）と `m`（質量）は、
その部品が持っている物理量で、工場で校正されています。
どこかの写真と比べて決めた相対値ではありません。

つまり加速度計の出力 `m/s²` は、**メートルという単位系に直結した絶対量**です。
よそから借りてきていません。

> **なぜ静止していても 9.8 を示すのか**
>
> 加速度計が測るのは**比力**（specific force）、
> 「重力以外の力による加速度」です。
> 机に置いた加速度計は、重力に逆らって机から支えられています。
> この支える力が測られるので、静止時に上向き 9.8 m/s² を示します。
> 自由落下させると支える力が消えるので **0** になります。

### 2-2. 2回積分すると距離になる

加速度が分かれば、時間で積分して速度、もう1回積分して位置が出ます。

```
v(t) = ∫ a dt        [m/s]
x(t) = ∫ v dt        [m]
```

この `x` が、さっきの式の `B`（カメラ間の距離）です。
**`B` にメートルが入るので、`Z = fx·B/d` の `Z` もメートルで出ます。**

マーカーも基準尺も置かずに実寸が出るのは、
スマホに「長さの単位を持った部品」が入っているからです。

### 2-3. なぜカメラと組み合わせるのか（VIO）

加速度計だけでは長続きしません。2回積分するので、
わずかな誤差が時間の2乗で膨らみます（**ドリフト**）。数秒で数メートルずれます。

一方カメラは、形と向きは長時間正確に追えますが、大きさが分かりません。

| | 得意 | 不得意 |
|---|---|---|
| カメラ | 形・向き（長時間安定） | 大きさ（原理的に不定） |
| 加速度計 | 大きさ（絶対単位） | 長時間（ドリフト） |

**弱点がちょうど逆**です。両方を組み合わせるのが
**VIO**（Visual-Inertial Odometry、視覚と慣性による自己位置推定）で、
ARCore のモーショントラッキングはこれです。

このプロジェクトは、ARCore が計算しているカメラ位置をそのまま貰ってきて、
写真と一緒に保存しているだけとも言えます。

---

## 3. 全体の流れ

```
[Android]                      [PC]
撮影アプリ                     frames_to_colmap.py
 ├ 連番JPEG        ──────►      └ 姿勢をCOLMAPの形式に変換
 └ frames.csv                        │
   （姿勢・地球座標）                 ▼
                                COLMAP
                                 ├ 特徴点を抽出
                                 ├ 画像間で突き合わせ
                                 └ 三角測量（姿勢は固定）
                                     │
                                     ▼
                                points_to_las.py
                                 └ 局所座標 → 平面直角座標＋標高
                                     │
                                     ▼
                                  .las
```

以下、それぞれを説明します。

---

## 4. 撮影アプリ — 姿勢付きの連番フレーム

### 4-1. なぜ動画ではないのか

「動画で撮って、あとからコマに割ればいいのでは」と思うところですが、採りませんでした。

- **フレーム間圧縮**が特徴点の手がかりを潰します。動画は「前のコマとの差分」で
  容量を稼ぐので、平坦に見える部分の情報が落ちます
- **ローリングシャッター歪み**。多くのスマホは画像を上から順に読み出すので、
  動きながら撮ると被写体が斜めに歪みます
- **1コマずつ姿勢と対応させられない**。これが決定的でした

なので、JPEGを連番で保存し、**その1枚1枚にカメラの位置と向きを添えて**います。

### 4-2. CSVに何を書くか

`frames.csv` の1行が1枚に対応します。主な列は次のとおりです。

| 列 | 中身 |
|---|---|
| `local_px/py/pz` | ARCore の局所座標系でのカメラ位置（メートル） |
| `local_qx/qy/qz/qw` | 同じくカメラの向き（四元数） |
| `lat`, `lon`, `alt_ellipsoid` | 地球座標（VPS由来）。緯度・経度・楕円体高 |
| `acc_h`, `acc_v`, `acc_yaw` | ARCore自身が申告する精度 |
| `fx`, `fy`, `cx`, `cy` | 内部パラメータ。**フレームごとに動く**ので毎回控える |
| `screen_orientation` | 端末の向き。後で効いてきます |

方針として、**計算した値は書かず、生の値だけ**を書いています。
現地で判断せず、全部持って帰ってPCで考える、という考え方です。

### 4-3. 踏んだ罠

- **XR Origin の Camera Y Offset**。既定値 `1.1176` がカメラのYに常時加算されます。
  「立っている人の目の高さ」を想定した値ですが、こちらは実測が欲しいので 0 にします
- **解像度**。ARCore経由で取れるフレームは処理用の解像度で、既定は 640×480 でした。
  実機で選べたのは 640×480 / 1280×720 / 1920×1080。1920×1080 を指定しています
- **排他**。地球座標のロガーと撮影を同時に走らせると、CSVの書き込みが競合します

---

## 5. 姿勢を渡す — `frames_to_colmap.py`

COLMAP は写真から自力でカメラ姿勢を推定できますが、
ここでは**ARCoreが出した姿勢を与えて、推定を飛ばします**。

理由は2つあります。**速い**のと、**実寸が保たれる**からです。
自力推定させると、さっきの `k` 倍の不定性がそのまま出てきます。

### 5-1. 座標系の変換（ここを間違えると鏡像になる）

Unity（ARCore）と COLMAP では約束事が違います。

| | Unity / ARCore | COLMAP |
|---|---|---|
| 世界座標 | 左手系 | 右手系 |
| カメラのY軸 | 上向き | 下向き |
| 姿勢の向き | カメラを世界に置く | 世界の点をカメラから見る（逆） |

左手系から右手系にするにはどれかの軸を反転します（`S = diag(1, 1, -1)`）。
カメラのYを上から下にするのも反転です（`C = diag(1, -1, 1)`）。

ここで注意が要ります。**反転（鏡映）を1回かけると、回転ではなくなります。**
行列式が `-1` になり、鏡に映した世界になってしまう。

ところが**2回かけると行列式は `+1` に戻ります**。

```
R_c2w = S · R_unity · C
```

世界側で1回、カメラ側で1回。**だから正しい回転になります。**
片方だけ直すと点群が鏡像になり、しかもぱっと見では気づきにくい。

### 5-2. 縦持ちの90度問題

もうひとつ、実データで踏んだものがあります。

AR Foundation が Unity のカメラに与える姿勢は「**画面の向き**」に合わせたものです。
一方、画像として取り出せるのは「**センサーそのままの横向き**」です。

端末を縦に持って撮ると、この2つが **90度ずれます**。
画像を回さずに保存している以上、姿勢の側で戻す必要があります。

視線軸まわりの回転なので、回転行列の列を組み替えるだけで済みます。

効果は劇的でした。

| | 起きた3次元点 |
|---|---|
| 補正なし | 2,815点 |
| 補正あり | **30,023点** |

10倍以上です。「点がほとんど起きない」ときは、まずここを疑ってください。

---

## 6. 三角測量 — COLMAP

姿勢を与えたので、COLMAP は特徴点の突き合わせと三角測量だけを行います。

同じ実測データで、姿勢を固定した場合と自力推定させた場合を比べました。

| | 姿勢を固定（ARCore） | COLMAPの自力推定 |
|---|---|---|
| 登録画像 | 286 / 286 | 245 / 286 |
| 3次元点 | 30,023 | 34,923 |
| 再投影誤差 | 1.80 px | **0.885 px** |
| 平均トラック長 | 3.61 | **9.49** |
| 所要時間 | **4.6分** | 46.8分 |

**姿勢を与えると10倍速く全枚数が通る代わりに、精度は自力推定に劣ります。**
ARCoreの姿勢は「向きは合っているが、写真から詰めた姿勢ほどの精度はない」わけです。

本来は初期値として渡して調整させるのが筋ですが、
そうすると実寸が保たれる保証が無くなります。ここはまだ宿題です。

> **トラック長**は「1つの3次元点が何枚の写真に写っていたか」です。
> 長いほど信頼できます。2枚しか写っていない点は誤対応でも成立してしまうので、
> 位置が不安定になります。

---

## 7. 座標を付ける — `points_to_las.py` と `jgd2011.py`

点群はまだ「起動地点が原点」の局所座標にいます。これを地図の座標に移します。

### 7-1. 楕円体高と標高は違う

地球は完全な球ではなく、赤道方向にわずかに膨らんだ回転楕円体です。
GPSが返す高さは、この**楕円体からの高さ**（楕円体高）です。

一方、地図や標識に書いてある「標高」は、**ジオイド**という面からの高さです。
ジオイドは平均海面を陸地まで延長した、重力の等しい面で、
地下の密度のむらでデコボコしています。

```
楕円体高 = 標高 + ジオイド高
```

日本付近のジオイド高は 30〜45m ほどあります。**無視できません。**
今回の撮影地では **37.75m** でした（国土地理院 GSIGEO2011）。

このプロジェクトでは当初 37.0m の定数を使っていて、
**Zが0.75mずれていました。** 場所ごとに確かめる必要があります。

### 7-2. 平面直角座標系

日本の測量では、全国を19の区域に分けて、それぞれに平面直角座標系を定めています。
球面を平面に押し付ける（投影する）ときのひずみを、狭い範囲に限ることで抑える仕組みです。

`tools/jgd2011.py` はこの変換を実装したものです。
**国土地理院の変換サービスと突き合わせて 0.1mm 差で一致**しました。
標準ライブラリだけで動くので、単体でも使えます。

### 7-3. 平行移動の近似ではだめな理由

「局所座標の原点を平面直角座標に変換して、あとは平行移動でいいのでは」
と思うところですが、これは効きます。

**子午線収差**という問題があります。
平面直角座標系の「北」と、その地点での本当の北（真北）は、
座標系の原点から東西に離れるほどずれます。

名古屋付近で **0.215度**ありました。小さく見えますが、

```
30 m × tan(0.215°) ≈ 11 cm
```

**30m先で11cmずれます。** cm級を狙うなら無視できません。

なので実装では、点を1つずつ

```
局所座標 → EUN（東・上・北） → ECEF（地球中心の直交座標）
        → 緯度経度 → 平面直角座標
```

と厳密に通しています。数千万点でも耐えるよう、リストを作らず
1点ずつ流し込む書き方にしています。

---

## 8. 精度をどう測ったか

ここからが、この記事のいちばん大事なところです。

### 8-1. 「精度」を1つの数字にしない

この手法の誤差は、性質の違う2つに分かれます。

| | 何が決まるか | 誤差源 |
|---|---|---|
| **相対** | 点群の形と大きさ | 加速度計、カメラ、三角測量 |
| **絶対** | 地球上のどこに、どの向きで置くか | VPS（測位） |

**桁がまったく違います。** 相対はcm〜%級、絶対はm級です。
これを混ぜて「精度◯m」と書くと、読んだ人は悪いほうの数字を
「この手法の精度」として受け取ってしまいます。

### 8-2. 実寸の検証 — 巻尺を撮る

相対精度、つまり実寸が本当に合っているかを確かめます。
**外部の物差しが要ります。**

黒い塀に巻尺を **173cm** 伸ばして貼り、そのまわりを歩きながら撮りました。

やったことは単純です。

1. 各コマで巻尺（暗い壁の上の細長い明るい帯）を見つけ、両端の画素位置を求める
2. ARCoreの**局所姿勢だけ**を使って視線を作り、両端をそれぞれ三角測量する
3. 2点間の距離を巻尺の読みと比べる

**VPSも緯度経度も使いません。** 加速度計とカメラで出した長さを、
物差しと突き合わせています。

端点は「明るさが半分に落ちる位置」で決めています。
ぼけが左右対称なら物理的な端に一致し、コマが変わっても同じ場所を指すためです。

**結果:**

| | |
|---|---|
| 三角測量による長さ | **172.6 cm** |
| 巻尺の読み | 173.0 cm |
| 差 | **−0.4 cm（−0.22%）** |

コマの選び方を変えると 170.7〜172.6cm に散りました。
ばらつき約2cmなので、**結論は「172 ± 1 cm」、実寸は約1%の精度で正しい**です。

正直に書くと、**「0.1%級で合っている」とは言えません。** 今回の測り方では
そこまで分解できません。どの区分でも 173cm より1〜2cm短く出ますが、
これは端点の決め方が内側に寄るためとも説明できて、区別がつきませんでした。

### 8-3. 危なかった話 — 基線長が足りないと何も測れない

最初、巻尺を貼った直後のコマだけを使って計算しました。結果は **136cm** でした。
21%も違う。焦りました。

原因は**基線長**です。三角測量は、2つの視点がどれだけ離れているかで精度が決まります。

奥行き `Z` の誤差は、視差 `d` の誤差 `σ_d` からこう伝わります。

```
Z = fx · B / d        より
σ_Z = Z² · σ_d / (fx · B)
```

**`Z` の2乗に比例し、基線長 `B` に反比例します。**
遠いものほど急激に悪化し、視点が離れていないほど悪化する。

実際の数字を入れてみます（`Z` = 1.7m、`fx` = 1388px、`σ_d` = 10px）。

| 使ったコマ | 基線長 `B` | 理論上の `σ_Z` | 実際の結果 |
|---|---|---|---|
| 貼った直後（立ち止まって撮影） | **0.115 m** | 約 18 cm（10%） | **135.9 cm** |
| 歩き回ったコマも含める | **0.723 m** | 約 3 cm（1.7%） | **172.6 cm** |

長さは奥行きに比例するので、奥行きが10%狂えば長さも10%狂います。

**基準物を貼って、その場から撮っただけでは何も測れません。**
今回は歩いたあとも巻尺が何度も画面に入っていたので救われました。

目安として **基線長 ÷ 対象までの距離 ≧ 0.3** を現地手順に書き足しました。
2m先のものなら、60cm以上は横に動く必要があります。

### 8-4. やってはいけない検証 — 自分と自分を比べる

これは自分がやらかした話です。

最初、こう考えました。

> ARCoreは2種類の位置を出す。局所座標（加速度計由来）と緯度経度（VPS由来）。
> **出どころが別**なのだから、両者の移動距離が一致すれば実寸の裏付けになるはずだ。

実際にやってみると、区間ごとの距離の比は **1.0000**。
「スケール差 0.1% で一致した」と書きました。

**これは誤りでした。**

確かめ方はこうです。区間の**前半だけ**で「局所座標→緯度経度」の変換を決め、
それを**後半に外挿**します。

- もし緯度経度が独立した測位なら … 後半では合わないはず。時間とともに離れていく
- もし局所座標に変換を掛けただけなら … いつまでも合う

結果は、**105秒・34.7m歩いても 2.7cm しかずれませんでした。**

自己申告の水平精度は 2.36m です。毎フレーム独立に測位しているなら、
この桁のばらつきが見えなければおかしい。見えませんでした。

**区間の中では、緯度経度は局所座標に固定の変換を掛けたものでした。**
同じ姿勢を2通りに書いたものを比べていたので、一致して当たり前だったのです。

「出どころが別」という思い込みが、検証になっていない検証を生みました。
**独立性は、疑って確かめるものです。**

---

## 9. ARCore の Geospatial は実際どう動いているか

前節の副産物として、公式には書かれていない挙動がいくつか見えました。

### 9-1. 新しい測位情報が入るのは一瞬だけ

`acc_yaw`（方位精度の自己申告）と、楕円体高から局所座標の上下動を引いた値を
並べると、**同じ時刻に同じ段差**が出ます。

| 時刻 | 高さの下駄 | 方位精度 |
|---|---|---|
| 63〜167秒 | 43.539 m で一定 | 8.66 → 8.84度（じわじわ悪化） |
| **167.0〜167.7秒** | **−0.503 m** | **8.84 → 4.43度** |
| 168〜378秒 | 43.039 m で一定 | 4.43 → 4.85度（じわじわ悪化） |

読み方はこうです。

- **段差 = VPSが照合し直した瞬間。** ここで位置・方位・高さがまとめて付け替わる
- **段差と段差の間は推測航法**。新しい観測が入らないので、
  自己申告の精度が毎分 0.113度 のペースで膨らんでいく

314秒・133mの撮影で、**段差は1回だけ**でした。
つまり地球に対する置き方の情報は、実質2回分しか入っていません。

前半と後半で当てはめ直すと、**方位が3.17度、位置が1.65m**違いました。
これが1回の撮影のあいだに「点群全体が回って動いた量」です。
自己申告の方位精度4.74度と同じ桁なので、申告値は信用してよさそうです。

**実用上の意味**: 現地に着いたらすぐ撮らず、歩いて周囲を映してから読む。
これは経験則として知られていますが、数字で裏が取れました。

### 9-2. 高さは測位で毎回求めた値ではない

楕円体高から局所座標の上下動（`local_py`）を引くと、
**フレーム間の変化が中央値 1.10mm** しかない定数が残りました。

**楕円体高は、局所座標の鉛直に定数を足したものです。**
その定数はVPSが照合し直したときだけ更新されます。

意味するところ:

- 垂直精度の値は「毎フレームの高さの精度」ではなく「足している定数の精度」
- 短時間の高低差は局所座標の精度（cm級）で信用してよい
- **絶対標高は定数まかせ**なので、別系統で与える必要がある

### 9-3. その定数はどこから来たのか

「高さは測位ではなく地形の標高データから引いているのでは」と疑っていました。
国土地理院の数値標高モデル（DEM、5mメッシュ）と突き合わせました。

```
DEM標高        中央値 3.15 m
楕円体高       中央値 43.5 m
その差         40.36 m
ジオイド高     37.75 m（GSIGEO2011）
→ 端末の地上高  2.61 m
```

端末を 1.4m の高さで持っていたとすると、**ARCoreの高さは +1.21m 高い**。

もし地形データから引いているなら、DEMとはcm級で噛み合うはずです。
噛み合いませんでした。**「地形データ由来」という疑いは支持されません。**

一方このずれは、自己申告の垂直精度 1.33m の内側に収まります。
**高さは測位由来で、1m級の偏りを持っている**と読むのが素直です。

そしてこれは、DEMを使えば机上で補正できます。

---

## 10. 数字の見取り図

最後に、全部を1枚にまとめます。

### 相対 — 形と大きさ

| | 値 | 根拠 |
|---|---|---|
| 実寸（スケール） | **1%以内** | 巻尺173cm → 172.6cm |
| 再投影誤差 | 1.6〜1.8 px | COLMAP |

### 絶対 — 置き場所と向き

| | 中央値 | 備考 |
|---|---|---|
| 水平位置 | 2.36 m | 68%信頼半径。3回に1回はこの外 |
| 方位 | 4.74 度 | **これが効く** |
| 高さ | 約1.2mの偏り | DEMで補正可能 |

方位誤差は距離に比例します。

| 基準点からの距離 | 4.74度によるずれ |
|---|---|
| 10 m | 0.83 m |
| 50 m | 4.15 m |
| 100 m | 8.30 m |
| 130 m | 10.78 m |

**「形は数%、置き場所は数m」** これがこの手法の実力です。

---

## 11. 落とし穴集

実際に踏んだものだけ書きます。

| 症状 | 原因 |
|---|---|
| 点がほとんど起きない | 縦持ちの90度ずれ。視線軸まわりの回転補正を忘れている |
| 点群が鏡像になる | 左手系→右手系の反転を片方しかかけていない |
| 実寸が2割ずれる | 基線長不足。立ち止まって撮っただけでは測れない |
| 標高が1〜2mずれる | ジオイド高を概算値のままにしている。場所ごとに調べる |
| 点群が2m平行移動する | 基準の緯度経度と基準の局所座標を、別々に中央値で取っていた（同じ1コマから取ること） |
| カメラの高さが1.1m高い | Unity XR Origin の Camera Y Offset の既定値 |
| CSVの1列目が読めない | BOM付き。`utf-8-sig` で開く |

---

## 12. まだできていないこと

正直に並べておきます。

- **レンズ歪みを補正していません。** AR Foundation が係数を返さないため、
  歪みなし（ピンホール）で近似しています。巻尺の検証では端点が画像の縁に
  来ることが多く、そこでの再投影残差が 10px ほどありました。
  歪みは画像の縁ほど大きいので、たぶんこれが主因です
  （COLMAPの再投影誤差 1.6〜1.8px は画像全体の平均なので、別の数字です）
- **トラック長が低い**（平均2.37、68%が2視点だけ）。同じ場所を一度しか
  通っていない撮り方が原因です。撮り方の問題なので直せます
- **姿勢を初期値として調整させる**やり方が未完成です。
  バンドル調整をかけると発散します（トラック長2の点に引きずられている）
- **高さが地形を追っているか**は未判定です。撮影地の起伏が
  0.51mしかなく、判定できませんでした。坂のある場所で撮り直せば片が付きます

---

## 13. まとめ

- 写真からは**原理的に大きさが決まりません**。すべてを `k` 倍しても同じ像になるからです
- どの分野も既知の長さを1本入れています。このプロジェクトでは
  **加速度計**がその役をします。ばね定数と質量は工場で校正された絶対量なので、
  出力の `m/s²` はメートルに直結しています
- 実際に測ったら、**実寸は1%以内**でした（巻尺173cm → 172.6cm）
- ただし**地球上のどこに置くかは数m級**です。この2つを混ぜないでください
- ARCore の緯度経度は**連続した測位ではありません**。
  区間内では局所座標に固定の変換を掛けたもので、新しい情報が入るのは
  VPSが解を組み直す瞬間だけでした（314秒で1回）

いちばんの学びは、最後のところです。
**「出どころが別だから独立だろう」は、確かめるまで思い込みでしかありません。**
外挿して合ってしまったとき、正直かなり驚きました。

---

## ツールと文書

すべて MIT ライセンスです。Python は標準ライブラリのみで動きます
（`measure_scale.py` だけ Pillow が要ります）。

| ファイル | 何をするか |
|---|---|
| `tools/frames_to_colmap.py` | 姿勢をCOLMAPの形式に変換。座標系の変換と縦持ち補正 |
| `tools/points_to_las.py` | 点群 → LAS 1.2（平面直角座標＋標高） |
| `tools/jgd2011.py` | 緯度経度 ⇔ 平面直角座標（全19系）。**単体で使えます** |
| `tools/analyze_accuracy.py` | 精度を相対と絶対に分けて測る |
| `tools/check_dem.py` | 国土地理院のDEM・ジオイド高と突き合わせる |
| `tools/measure_scale.py` | 巻尺から実寸を検証する |

| 文書 | 中身 |
|---|---|
| `THEORY.md` | 原理の解説。この記事より詳しい導出があります |
| `PHASE4_ACCURACY.md` | 精度の実測と考察の全文 |
| `FIELD_CHECKLIST.md` | 現地作業の手順 |

---

*この記事の数値はすべて実測です。撮影は Google Pixel 9a、
点群生成は COLMAP、検証データは 2026年8月の屋外実測によります。*
