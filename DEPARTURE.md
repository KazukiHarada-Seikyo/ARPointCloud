# 出発準備リスト（2026-08-12 の屋外実測）

夜のうちにやることと、朝やることを分けてある。
現地での手順は `FIELD_CHECKLIST.md`。

---

## 今夜（PCの前で）

### 1. 端末をつないでデータを空ける

```bash
cd C:\Users\harak\ARPointCloud; .\tools\pull.ps1
```

- [ ] `Captures\rec_*` に入ったことを確認
- [ ] 端末側を消す（アプリの削除ボタンでも、下のコマンドでも）

```bash
& "C:\Program Files\Unity\Hub\Editor\6000.5.6f1\Editor\Data\PlaybackEngines\AndroidPlayer\SDK\platform-tools\adb.exe" shell "rm -rf /sdcard/Android/data/jp.seikyo.arpointcloud/files/rec_* /sdcard/Android/data/jp.seikyo.arpointcloud/files/geolog_*.csv"
```

- [ ] **空き容量が10GB以上あること**（1080p・10fpsで1分あたり約300MB）

### 2. ビルドして入れる

- [ ] Unity で Console にエラーが無い
- [ ] Build して端末に入れる
- [ ] **入れたAPKが今日の修正を含んでいるか**を、次の3点で確認する

| 見るところ | 期待 |
|---|---|
| 起動して数秒 | 「解像度を1920x1080に設定しました」が**5秒間**出る |
| 削除ボタンを押す | 「もう一度押すと削除します / …」が出る（1回では消えない） |
| `Record` を押して止める | **共有シートが出る** |

3つとも今日入れたものなので、**どれか出なければ古いAPKが入っている。**

### 3. 動作を通しで確認（5分）

- [ ] 録画ボタン → 赤丸が角丸の四角になり、ゆっくり脈打つ
- [ ] もう一度押す → 丸に戻る
- [ ] メモボタン → `-` → `1F` → `2F` … と回り、色が黄色に変わる
- [ ] `i` ボタン → 数値が出る／消える
- [ ] 粒が見える（模様のあるものを映す。白い壁では出ない）
- [ ] **端末を放置して画面が消えない**

最後の1つが効いていないと、屋外で数分かざしている間に撮影が止まる。
**ここだけは必ず確認する。**

### 4. 充電と荷造り

- [ ] 端末を100%まで充電
- [ ] **モバイルバッテリーを充電**（一番忘れやすい）
- [ ] ケーブルをカバンに入れる
- [ ] 保冷剤を冷凍庫に入れる

---

## 明日の朝（出る前）

- [ ] 端末の電池 100%
- [ ] モバイルバッテリー 100%
- [ ] 空き容量 10GB以上（画面の `i` で確認できる）
- [ ] 自動回転オフ（アプリ側でも縦に固定しているが念のため）
- [ ] 画面の明るさ最大
- [ ] 飲み物、塩分、日傘か帽子
- [ ] 保冷剤とタオル
- [ ] 紙のメモとペン

### 行き先を決めておく

- [ ] **1階と上階の両方に立てる建物**（§6-1の検証に要る）
- [ ] ストリートビュー車が通った道から見える場所
- [ ] 日陰で休める場所が近くにある

---

## 今日わかっていること（現地で迷わないために）

| 場面 | 判断 |
|---|---|
| 到着直後 | 方位精度は42度まで悪い。**10秒歩いてから**画面を見る |
| 「方位を調整中」 | まだ撮らない。歩いて周りの建物を映す |
| 「撮影できます」 | 方位5度以下。VPSが効いている |
| 「撮れます（精度は不十分）」 | 撮ってよいが、精度は期待できない |
| 「位置が止まっています」 | 外挿の疑い。場所を変える |
| 録画中「速すぎます」 | 手の動きを落とす |
| 見送りが増える | 端末の発熱を疑う。日陰で冷ます |

**1回の撮影は数分まで。** 長く撮るほど発熱・容量・トラッキング破綻の
確率が上がる。

---

## この回で答えを出したい問い

1. 方位精度は本当に下がるか（案内文が役に立つか）
2. **高さは測位由来か、地形データ由来か**（最重要・§6-1）
3. 屋外の明るさでボタンと文字が見えるか
4. 撮った写真から点群ができるか（帰宅後）

2の手順は `FIELD_CHECKLIST.md` §4 に書いてある。
**メモボタンで階を設定してから `Record`** を押すこと。忘れると区別がつかない。

---

## 帰ってきたら

```bash
cd C:\Users\harak\ARPointCloud; .\tools\pull.ps1
```

そのあと疎な点群まで（開発機のCPUで約5分）。

```bash
python tools\frames_to_colmap.py Captures\rec_YYYYMMDD_HHMMSS
```

続きは `PHASE2_COLMAP.md` の手順2から。

---

## 現在の状態（2026-08-11 夜の時点）

コード側は確認済み。

- C#の構文チェック 10ファイルすべて問題なし
- Unityのコンパイル成功（20:18、最後の修正を含む）
- Console にコンパイルエラーなし
- `UICircle` 4か所すべて指定どおり
- OnClick 5か所すべて正しい
- `CaptureButtons` の接続5つすべて完了
- シェーダ登録2つとも入っている
- `Panel` は最背面に移動済み（削除ボタンを塞がない）

**未確認は実機での動作のみ。** 上の「3. 動作を通しで確認」がそれにあたる。

### 残っている小さなこと（急がない）

- `Panel` の Raycast Target がまだオン。いまは最背面なので実害はないが、
  並び順が変わると再発する。`Tools → ARPointCloud → 撮影画面のUIを仕上げる`
  を実行すれば切れる
- `Record` ボタンの文字が「ImageDebug」のまま
- `FrameRecord` に `UIButtonJuice` が付いていない（押しても弾まない）
- `Inner` に `UIButtonJuice` が付いている（Raycastオフなので何も起きない）
