# フェーズ4 精度測定

最終更新: 2026-08-13
対象データ: `Captures/files/rec_20260812_111139`（屋外実測・2,732枚・314秒・経路133.5m）
道具: `tools/analyze_accuracy.py` / `tools/check_dem.py` / `tools/measure_scale.py`

```bash
python tools\analyze_accuracy.py Captures\files\rec_20260812_111139
```

---

## 0. 先に結論

1. **緯度経度は独立な測位ではない。** 区間の中では、局所座標に固定の変換を
   掛けたものにすぎない。したがって**両者を突き合わせても実寸の裏付けにならない**。
   2026-08-12 に「スケール差 +0.10% で実証した」と書いたのは誤り（§2）
2. **楕円体高も同じ。** `alt_ellipsoid` は `local_py` に定数を足しただけ。
   さらにDEMと突き合わせた結果、**その定数は地形データ由来ではなかった**。
   測位由来で +1.21 m の偏りを持つ。ROADMAP §6-1 に答えが出た（§3）
3. **VPSが新しい情報を入れるのは、解を組み直す瞬間だけ。** 314秒の撮影で1回。
   そのとき方位が3.17度回り、高さが0.50m付け替わった（§1）
4. **`points_to_las.py` の基準の取り方に不備があった → 修正済み。**
   点群は実際に 1.96 m 動いた（§5）
5. **ジオイド高の既定値 37.0 m はこの場所では 0.75 m 足りない。**
   正しくは 37.75 m（§3）
6. **実寸は外部の基準で裏付けが取れた（§7）。** 巻尺173cmに対し172.6cm、
   約1%の精度で一致。ただし0.1%級の主張はこの精度ではできない

相対（形と大きさ）と絶対（置き場所と向き）を分けて書く方針そのものは変わらない。
**8/12の時点では「相対の正しさを手持ちのデータでは示せない」状態だったが、
8/13の巻尺で外部の裏付けが取れた（§7）。**

---

## 1. VPSは連続では効かない

`acc_yaw`（方位精度の自己申告）と、`alt_ellipsoid - local_py`（高さの下駄）を
並べると、同じ時刻に同じ段差が出る。

| 時刻 | 高さの下駄 | 方位精度 |
|---|---|---|
| 63〜167秒 | 43.539 m で一定 | 8.66 → 8.84度（じわじわ悪化） |
| **167.0〜167.7秒** | **−0.503 m** | **8.84 → 4.43度** |
| 168〜378秒 | 43.039 m で一定 | 4.43 → 4.85度（じわじわ悪化） |

読み方はこうなる。

- **段差＝VPSが照合し直した瞬間。** ここで位置・方位・高さがまとめて付け替わる
- **段差と段差のあいだは推測航法。** 新しい観測が入らないので、
  自己申告の精度が毎分 +0.113度 のペースで膨らんでいく

この撮影では段差が1回しかない。つまり **314秒・133m の撮影で、地球に対する
置き方の情報は実質2回分**（開始時と167秒時点）しか入っていない。

区間ごとに局所座標をENUへ当てはめると:

| 区間 | 倍率 | 方位（局所+X軸） | 残差 中央値 | 残差 最大 |
|---|---|---|---|---|
| 63〜168秒 | 1.0025 | 52.02度 | 0.031 m | 1.941 m |
| 168〜378秒 | 1.0003 | 55.19度 | 0.027 m | 0.182 m |

**2つの区間で方位が3.17度、位置が1.65m違う。** これが1回の撮影のあいだに
「点群全体が回って動いた量」で、自己申告の方位精度4.74度と同じ桁に収まっている。
申告値は信用してよい、という裏付けになる。

> **またいで測ってはいけない。** 区間をまたいだ組で距離の比を取ると、
> 20〜50mの組で 0.95 になる。これはスケール誤差ではなく座標の付け替えを
> 拾っただけ。区間で切らずに測ると、相対精度を実際より悪く見積もる。

---

## 2. 「実寸の独立検証」は成立していない

### やったこと

区間の**前半だけ**で「局所座標→緯度経度」の変換を決め、それを**後半に外挿**する。

- 緯度経度が独立な測位なら … 後半では合わない。時間とともに離れていくはず
- 局所座標に変換を掛けただけなら … いつまでも合う

### 結果

| 区間 | 外挿の誤差 中央値 | 最大 | 経過 | 歩いた距離 |
|---|---|---|---|---|
| 63〜168秒 | 7.2 cm | 197.7 cm | 52秒 | 23.9 m |
| **168〜378秒** | **2.7 cm** | 18.0 cm | 105秒 | 34.7 m |

フレームからフレームへの移動量も比べた。

| 区間 | 局所 | VPS | 差の中央値 |
|---|---|---|---|
| 168〜378秒 | 47.0 mm | 46.2 mm | **2.44 mm** |

**105秒・34.7m歩いても、前半で決めた変換が 2.7 cm しか外れない。**
自己申告の水平精度は 2.36 m である。もし緯度経度が毎フレーム測位した独立な値なら、
この桁のばらつきが見えなければおかしい。見えない。

### 意味

区間の中では **緯度経度 = 局所座標 × 固定の変換**。同じ姿勢を2通りに書いただけ。
だから距離の比を取れば 1.0000 になる。**一致して当たり前**で、
そこに実寸の情報は入っていない。

参考までに区間内の比は以下（読み方に注意すること）。

| 離れ方 | 組数 | 比の中央値 | ばらつき |
|---|---|---|---|
| 2〜5 m | 13,493 | 1.0010 | ±1.67% |
| 5〜10 m | 27,199 | 1.0008 | ±0.77% |
| 10〜20 m | 29,411 | 1.0011 | ±0.39% |
| 20〜50 m | 18,886 | 1.0000 | ±0.24% |

ここで言えるのは「変換の当てはめと座標変換の実装に破綻がない」ことまで。
**`THEORY.md` §11（加速度計が絶対単位のスケールを与える）という理屈自体は
変わらない。変わったのは「このデータで実証した」と言えない点だけ**である。

> 屋内データ（`rec_20260811_*`）でも同じ検定を通したが、
> 移動が1〜2mしかなく判定に使えない。ツールはその場合そう表示する。
> 現状、この判定を支えているのは0812の1本だけ。

---

## 3. 高さの出どころ（ROADMAP §6-1 への回答）

`alt_ellipsoid` から `local_py` を引くと、ほぼ動かない定数が残る。

```
差の変化量  中央値 1.10 mm/フレーム  (99.3% が 20mm以下)
0.05 m を超える付け替え  5回 / 2,731フレーム
```

`local_py` の範囲が 0.890 m、`alt_ellipsoid` の範囲が 1.121 m。
**楕円体高の上下動は、端末そのものの上下動でほぼ説明がつく。**

### 結論

**高さは測位で毎回求めた値ではない。** 局所座標の鉛直に定数を足したもので、
その定数はVPSが照合し直したときだけ更新される。

- 垂直精度 1.33 m は「毎フレームの高さの精度」ではなく「足している定数の精度」
- 短時間の高低差は局所座標の精度（cm級）で信用してよい
- 絶対標高は定数まかせ。**別系統で与える設計に変える**

当初予定していた「1階と上階で楕円体高を読み比べる」現地作業は**不要になった**。
同じことが1回の平地の撮影から読み取れた。

### 定数はどこから来たのか — DEMとの突合

`tools/check_dem.py` で国土地理院の標高タイル（DEM5A・5mメッシュ）と突き合わせた。

```bash
python tools\check_dem.py Captures\files\rec_20260812_111139
```

```
DEM標高          2.89 〜 3.40 m（中央値 3.15 m、起伏はわずか0.51m）
楕円体高        43.06 〜 44.10 m
その差          中央値 40.36 m
ジオイド高      37.7517 m（GSIGEO2011・国土地理院より取得）
→ 端末の地上高  2.61 m
```

端末を 1.4 m の高さで持っていたとすると、**ARCoreの高さは +1.21 m 高い**。

**もし定数が地形の標高データから引かれているなら、DEMとはcm級で噛み合うはず。
噛み合っていない。当初の「地形データ由来」という疑いは支持されない。**

一方このずれ 1.21 m は、自己申告の垂直精度 1.33 m の内側に収まる。
**高さは測位由来で、1m級の偏りを持っている**と読むのが素直。

> 前提: 端末を 1.4 m で持っていたという想定に依存する。
> `--hold` で変えられる。2.6 m で持って歩いていたなら話は変わるが、
> 手持ちで歩いた撮影なので現実的ではない。
>
> 「地形の起伏を追っているか」は、この撮影地の起伏が 0.51 m しかないため
> **判定できない**。起伏のある場所で撮れば分かる。

### ついでに見つかった: ジオイド高の既定値が違う

この場所の正しいジオイド高は **37.75 m**。`points_to_las.py` の既定は 37.0 m
なので、**そのまま使うとZが 0.75 m ずれる**（ROADMAP §6-5 の宿題が数字になった）。

- `--geoid 37.75` … 正しいジオイド高で書き出す（**採用済み**）
- `--geoid 38.96` … さらにARCoreの偏り +1.21 m も打ち消し、地面をDEMに合わせる

後者は「高さだけの位置合わせ」で、フェーズ5の一部を机上で済ませたことになる。
ただし持ち高 1.4 m の想定が入るので、既定にはしていない。

---

## 4. 絶対精度（自己申告と、その使い方）

| 項目 | 中央値 | 5% | 95% |
|---|---|---|---|
| 水平精度 | 2.36 m | 2.30 | 3.21 |
| 垂直精度 | 1.33 m | 1.26 | 1.38 |
| 方位精度 | 4.74度 | 4.46 | 8.84 |

いずれも68%信頼半径。3回に1回はこの外に出る。

方位誤差は距離に比例して効く。

| 基準点からの距離 | 4.74度のとき | 最良の4.43度でも |
|---|---|---|
| 10 m | 0.83 m | 0.77 m |
| 50 m | 4.15 m | 3.87 m |
| 100 m | 8.30 m | 7.75 m |
| 130 m | 10.78 m | 10.07 m |

**記事では必ず分けて書く。** 「形は良い、置き場所は数m〜10m級」であって、
混ぜると「精度10m」と読まれる。

---

## 5. `points_to_las.py` の基準の取り方（修正済み）

修正前の実装は基準をこう決めていた。

```python
lat0 = sorted(lat)[中央]      # 緯度だけ並べ替えた中央値
lon0 = sorted(lon)[中央]      # 経度だけ並べ替えた中央値
ref_local = (sorted(px)[中央], sorted(py)[中央], sorted(pz)[中央])
q_w2eun = q_average(全コマ)   # 全コマの平均回転
```

問題が2つあった。

**(1) 基準の緯度経度と基準の局所座標が、別のコマを指している。**
それぞれ独立に並べ替えているので、経路が曲がっていると同じ瞬間にならない。
このデータでは t=219.98秒 と t=225.64秒 のコマを同じ点として結んでいた。

→ **点群全体が 2.22 m 平行移動する。**

**(2) 方位を全コマ平均で決めている。**
§1のとおり方位は途中で3.17度付け替わる。平均すると古い解と新しい解の
中間になり、どちらでもない向きになる。

```
q_average による方位     53.98度
最後の区間の当てはめ     55.19度   → -1.21度、50m先で 1.05 m
```

### 対処 — **修正済み（2026-08-12 夜）**

- 基準は**1つのコマ**から取るようにした（緯度経度と局所座標を同じ行から読む）。
  区間の重心に最も近いコマを選ぶ。回転誤差の腕が最短になるため
- 方位と基準を**最後の再照合以降の区間**で決めるようにした。
  新たに `last_refix_index()` を追加。以前の挙動は `--use-all-frames` で再現できる

```
最新のVPS解の区間 1824 / 2732 コマを基準に使います
局所→EUN回転のばらつき 中央値 0.58度 / 最大 10.25度
基準点 35.234, 136.794  楕円体高 43.918 m  (elapsed 373.5 秒のコマ)
```

書き出し直した結果、点群は実際に動いた。

| | 旧 | 新 | 差 |
|---|---|---|---|
| 中心（東） | — | — | **+1.85 m** |
| 中心（北） | — | — | **−0.64 m** |
| 水平の移動 | | | **1.96 m** |

X方向の端が +2.30 m と +1.40 m で違う量だけ動いているのは、
平行移動だけでなく 1.21度 の回転が入ったため。

### 現在の成果物

```bash
python tools\points_to_las.py Captures\work_0812_step3\sparse_tri ^
    Captures\files\rec_20260812_111139 --zone 7 --geoid 37.75
```

`Captures/files/rec_20260812_111139/pointcloud_zone7.las`
121,793点 / 3.0 MB / EPSG:6675 / X=東, Y=北 / ジオイド高 37.75 m

---

## 6. 次にやること

### 実寸の裏付け — **済み（§7）**

2026-08-13に巻尺で検証した。**172.6 cm / 実測173 cm、約1%で一致。**
より精度を上げるなら、マーカーか PLATEAU の建物外形。

### 高さの位置合わせ（やるなら）

`--geoid 38.96` で書き出すと、地面がDEMの標高に合う。
ARCoreの +1.21 m の偏りを打ち消した版で、フェーズ5の高さ成分を
机上で済ませたことになる。持ち高 1.4 m の想定が入る点だけ注意。

### 起伏のある場所での再測定

「ARCoreの高さが地形を追っているか」は、今回の撮影地の起伏が
0.51 m しかないため判定できていない。坂のある場所で1本撮れば片が付く。

### 密な点群が戻ってきたら

```bash
python tools\points_to_las.py <fused.ply> Captures\files\rec_20260812_111139 ^
    --zone 7 --geoid 37.75 --max-distance 30
```

`--geoid 37.75` を忘れないこと（既定の37.0のままだとZが0.75mずれる）。

### 記事に使える形

`analyze_accuracy.py` の出力がそのまま素材になる。特に §2 の外挿テストは
「ARCoreのGeospatialが実際どう動いているか」を示す図として強い。

---

## 7. 実寸の裏付け（2026-08-13・巻尺）

**フェーズ4の最後の穴が埋まった。** 外部の基準に対する、初めての実寸検証。

道具: `tools/measure_scale.py`　データ: `Captures/rec_20260813_104042`

```bash
python tools\measure_scale.py Captures\rec_20260813_104042 --true-length 1.73
```

### やり方

黒い塀に巻尺を 173 cm 伸ばして貼り、そのまわりを歩きながら撮った。

1. 各コマで巻尺(暗い壁の上の細長い明るい帯)を見つけ、両端を
   「帯らしさが半分になる位置」でサブピクセルに決める
2. ARCoreの**局所姿勢**から視線を作り、両端をそれぞれ三角測量する
3. 2点間の距離を巻尺の読みと比べる

**VPSも緯度経度も使っていない。** 加速度計とカメラだけで出した長さを、
物差しと突き合わせている。§2 で「局所座標とVPSの一致は実寸の裏付けに
ならない」と分かった以上、外部の物差ししか手が無い。

### 結果

| | |
|---|---|
| 三角測量による長さ | **172.6 cm** |
| 巻尺の読み | 173.0 cm |
| 差 | **−0.4 cm（−0.22%）** |
| 使ったコマ | 42枚 / 基線長 0.72 m / 壁まで 1.35〜1.75 m |

コマの選び方を変えた場合:

| 区分 | 枚数 | 長さ |
|---|---|---|
| 全部 | 42 | 172.6 cm |
| 中盤のみ(#295-490) | 7 | 172.2 cm |
| 終盤のみ(#770-825) | 22 | 170.7 cm |
| 偶数コマ / 奇数コマ | 24 / 21 | 171.6 / 172.1 cm |

**ばらつきは約2cm。したがって結論は「172 ± 1 cm」、
実寸は約1%の精度で正しい。**

### どこまで言えるか

- **言える**: ARCoreの実寸は**1%の精度で正しい**。100m で 1m 以内
- **言えない**: 「+0.10%」のような0.1%級の主張。今回の精度では区別できない
- どの区分でも 173 cm より **1〜2 cm 短く**出るが、これは端点を
  「輝度が半分に落ちる位置」で定義しているぶん内側に寄るためと考えられる。
  本当に1%短いのか、測り方の偏りなのかは、この測定では分けられない

再投影残差の中央値が 10.7 px と大きめなのは、レンズ歪みを
PINHOLEで近似しているため(ROADMAP §7 の宿題)と見られる。

### 撮り方の教訓（重要）

**同じデータでも、最初の45コマだけを使うと 136 cm になる。**

| 使ったコマ | 基線長 | 結果 |
|---|---|---|
| #0〜45（貼った直後、立ち止まって撮影） | **0.07 m** | **136 cm** ← 破綻 |
| 歩き回った42コマ | 0.72 m | 172.6 cm |

三角測量は基線長で決まる。**基準物を貼って、その場から撮っただけでは
何も測れない。** 幸い今回は歩いたあとも巻尺が何度も画面に入っていたので
救われた。`FIELD_CHECKLIST.md` に追記した。

  目安: **基線長 / 対象までの距離 ≥ 0.3**
