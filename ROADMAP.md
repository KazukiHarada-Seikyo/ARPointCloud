# ARPointCloud ロードマップ / 引き継ぎ文書

最終更新: 2026-08-10（実装との突き合わせで訂正）
対象: このプロジェクトを引き継ぐAIモデル・将来の自分

---

## 1. ゴール

スマホ(ARCore)のカメラ位置と実寸を利用し、連続取り込みした画像から点群を生成し、**平面直角座標系＋標高の座標が付いたLASファイル**として出力するツールを作る。OSS(GitHub)＋Qiita記事として公開する。

- 中心テーマは「**この手法で精度をどう上げるか**」。精度の実測と考察が記事の骨になる
- 出力LASは、既公開のOSS「Point Cloud Kit」(Unity向けLAS点群インポータ)で読める形にする。作る側と見る側を2本のOSSで繋げる
- 狙いは (1) 読者に点群を一度体験してもらう (2) OSSとして実際に価値のあるものにする

## 2. 前提・制約

- 休日の個人プロジェクト。**ハードルの低さを最重視**。現地へ撮りに行く／マーカーを1枚置く、までは許容。多数の対空標識の設置はやらない
- 端末: Google Pixel 9a (LiDAR非搭載。深度センサー前提の設計は不可)
- 開発主機: Core Ultra 5 / 24GB / 512GB SSD(内蔵GPU)。Unity・コード・クラウド管理はこちら
- 点群生成の計算と点群の表示確認: RTX 5070機。**画像・中間ファイルはUltra 5機に置かない**(容量が膨らむため)
- Unity 6000.5.6f1 / AR Foundation 6.5.0 + ARCore XR Plugin 6.5.0 + ARCore Extensions 1.54.0(arf6ブランチ)
- パッケージ名: jp.seikyo.arpointcloud
- 猛暑のため、**屋内でできる作業をフェーズ順に関係なく先に全部済ませ、屋外実測は1回にまとめる**

## 3. 全体構造(なぜこの形か)

ARCoreは2種類の位置を出す。

1. **ローカル姿勢**(モーショントラッキング): 起動地点原点・メートル実寸。写真→点群の実寸付与はこれが担う。ただし水平の向きは起動時のスマホの向きで毎回変わる(Y軸のみ重力で固定)
2. **地球座標**(Geospatial API / VPS): ローカル座標系が地球上のどこに・どの向きで置かれているかを与える。平面直角座標系への変換はこれに乗る

精度は3つの独立した問題として扱う:

| 成分 | 主な誤差源 | 状態 |
|---|---|---|
| 水平位置 | GNSS + VPS | VPS成立時は1m前後の実測あり |
| 方位 | VPS(不成立時はコンパス水準まで悪化) | 点群全体が回る。50m先で1度≒0.9m。最重要 |
| 高さ | **測位ではなく地形標高データ由来の疑い** | 未検証。§6参照 |

## 4. フェーズと現在地

- **フェーズ0: 実地確認とログ収集の仕組み** — ほぼ完了
  - 完了: カメラ表示ビルド / Geospatial有効化 / 4段ステータス表示(端末対応・EarthState・Tracking・値) / CSVロガー / NativeShare共有 / VPSカバレッジ問い合わせ
  - 残り: 階を変えた楕円体高の検証(屋内で可能・§6-1)、屋外での本実測(1回にまとめる)
- **フェーズ1: 撮影アプリ** — 次はここ
  - 「連番写真をシャッターで撮る」ではなく**動画的な連続取り込み**。操作はかざして動かすだけ、保存は**姿勢付き連番フレーム**(動画ファイルにはしない。§6-2)
  - 保存レートの初期値: 10fps程度。現地での取捨選択はしない(全部持って帰る)
  - 撮影中は `frames.csv` 一本。`GeospatialCsvLogger` とは排他(列が包含関係にあり、書き込みも競合するため)
  - 1080p JPEGは300〜500KB/枚。10fpsで1分あたり200〜300MB。1回の撮影は数分が上限
- **フェーズ2: PC側で点群生成** — 疎な点群まで開発機で実証済み。密な点群(CUDA必須)が5070機の仕事
  - ARCoreのローカル姿勢を与え、SfM(姿勢推定)を飛ばして三角測量から始める。実寸はARCore由来なので倍率不定の問題が起きない
  - `tools/frames_to_colmap.py` — frames.csv → COLMAPテキストモデル。座標系の変換(Unity左手系・カメラY上 → COLMAP右手系・カメラY下)と、視線軸まわりの回転補正
  - 手順と実測値は `PHASE2_COLMAP.md`
  - **実測(286枚)**: 姿勢固定 → 全286枚登録・30,023点・再投影誤差1.80px・4.6分。
    COLMAPの自力推定 → 245枚登録・34,923点・再投影誤差0.885px・46.8分。
    **姿勢を与えると10倍速く全枚数が通るが、精度は自力推定に劣る。**
    初期値として渡して調整させる形が本来の姿(§6-4)
- **フェーズ3: 座標変換とLAS出力** — 通し済み。ジオイドが宿題(§6-5)
  - `tools/jgd2011.py` — 緯度経度⇔平面直角座標(GRS80/JGD2011、全19系)。
    **国土地理院の変換サービスと突合して0.1mm差で一致**。原点は全19系で(0,0)、往復誤差3nm、原点の縮尺0.9999000
  - `tools/points_to_las.py` — COLMAPの点群 or PLY → LAS 1.2 形式2。
    局所座標→EUN→ECEF→緯度経度→投影と1点ずつ厳密に変換する
    (子午線収差が名古屋で0.215度あり、平行移動の近似では30m先で11cmずれるため)
  - 出力は `UnityLasImporter` が読む形式に合わせた。VLR に LASF_Projection / 34735 /
    ProjectedCSTypeGeoKey = EPSG。軸順は **X=東, Y=北**(右手系になるため。§8参照)
  - 実測: 30,023点を EPSG:6675 で書き出し、読み戻して検証済み
- **フェーズ4: 精度測定** — マーカー1枚を答え合わせに使う
  - **相対（形と大きさ）と絶対（置き場所と向き）を分けて測ること。**
    2026-08-12の実測では、スケールは VPS と +0.10% で一致した一方、
    方位精度4.74度がそのまま乗るため、基準点から50m先で約4m、
    130m先では約10mずれる。混ぜて書くと「精度10m」と読まれてしまう
  - マーカーで測れるのは前者。後者を詰めるにはフェーズ5が要る
- **フェーズ5: 既存公開点群への位置合わせ** — 精度を詰める手段。高さ問題の受け皿になる可能性
  - **遠征は不要かもしれない。** 大規模点群(静岡など)を求めると遠征になるが、
    「既知の基準に合わせる」目的なら愛知で足りる候補がある
  - **国土地理院の数値標高モデル(DEM)** — 5m/1mメッシュ、全国、無償。点群ではなく
    地面の標高だが、§6-1の検証には直接的。ARCoreの標高が地形データ由来なら、
    同じ緯度経度でDEMを引いた値と不自然なほど一致するはず。
    **今日のデータで机上で検証できる。現地に行く必要がない**
  - **PLATEAU(3D都市モデル)** — 名古屋市を含む愛知の複数市をカバー、無償。
    建物形状なので位置合わせに使える
  - どちらも実際の収録範囲は要確認
- **フェーズ6: GitHub + Qiita公開**
  - 注意: APIキーが ProjectSettings/ARCoreExtensionsProjectSettings.json に平文で入る。公開前にAndroidアプリ制限(パッケージ名+SHA-1)をかけるか、キーレス認証に移行する
  - **このファイルは最初のコミットからgit除外する**。一度履歴に入ると消すのが面倒なため。フェーズ1のStep 0で対応

## 5. 実測で分かったこと(設計判断の根拠)

1. **屋内でも4段すべて成立し、値は返ってくる**。「値が出る=VPSが効いている」ではない
2. 場所A(屋内): 水平1.30m/方位2.82度。場所B(屋内): 水平7.03m/方位27.8度。**同一アプリ・同一端末で場所だけ違う**
3. 場所Bでは実移動0.94mに対し緯度経度が8.77m漂った。精度値の自己申告(7m)と実際の漂い(東西7.2m)はよく一致 → 精度値は信用してよい
4. 精度値は**68%信頼半径**。3回に1回はこの範囲を外れる。最悪値は2〜3倍を見る
5. 方位精度は時間で改善する(42度→17.5度/約10秒)。**現地到着直後の値は悪い。歩いて周囲を映してから読む**
6. カバレッジ問い合わせ(CheckVpsAvailabilityAsync)は両地点ともAvailable。**候補地の絞り込みには実質使えない**。「地図があるか」しか答えず「今映しているもので照合できるか」は答えない
7. 撮影地の条件: ストリートビュー車が通った道から見える場所が最良。開けた場所はGNSSに有利だがVPSが効きにくい。建物裏・路地奥は両方弱い
8. XR OriginのCamera Y Offset既定値1.1176がカメラYに常時加算される罠。0に設定済み

## 6. 未解決の重要課題

### 6-1. 高さの出どころ(最優先・**屋外で行う**)
垂直精度(1.14m)が水平(7m)より良いのは不自然。高さは測位ではなく**地形の標高データから引いている疑い**。同じ建物の1階と上階で楕円体高を読み比べる:

> 当初は「屋内で検証可能」としていたが誤り。屋内ではVPSが効いている保証がないため、
> 値が変わらなくてもそれが地形由来なのか単にVPSが不成立なだけなのか切り分けられない。
> **VPSが成立している状態を作れる屋外で行う**。屋外に出る回にまとめる。
- 階の高さ分変わる → 測位由来。信用できる
- ほぼ変わらない → 地形由来。**上階では系統誤差になり精度値に現れない**。高さは別系統(既知点合わせ・フェーズ5の公開点群合わせ等)で与える設計に変える

また上階ではVPS照合が原理的に成立しない(ストリートビューは地上から撮影)。

### 6-2. 解像度 vs 姿勢対応 — 決着済み
ARCore経由のフレームは処理用解像度で、カメラ本来の48MPに遠く及ばない。一方、別撮りの高解像度写真は姿勢との対応が時刻合わせ頼みになる(数十msのずれ=歩行中数cm)。

実機確認の結果、Pixel 9aで選べるのは **640×480 / 1280×720 / 1920×1080(すべて30fps)**、既定は640×480。**1920×1080のARCoreフレームを使う方針で確定**(別撮りは採らない)。1920×1080は4:3から上下180ずつ対称にクロップされた16:9で、垂直視野が約55度→約42度に狭まる点に注意。詳細は `HANDOFF_APP.md` §3。

なお動画ファイル保存を採らない理由: フレーム間圧縮が特徴点の手がかりを潰す / ローリングシャッター歪み / 姿勢と1コマずつ対応させられない。

### 6-4. ARCoreの姿勢を「固定」するか「初期値」にするか(未解決)
実測(286枚)では、姿勢を固定すると全枚数が10倍速く通る代わりに、再投影誤差が
COLMAPの自力推定の約2倍(1.80px 対 0.885px)、トラック長は3分の1(3.61 対 9.49)になる。
ARCoreの姿勢は向きは合っているが、写真から詰めた姿勢ほどの精度は無い。

理想は初期値として渡して調整させる形だが、以下は未解決:

- `bundle_adjuster` を既定設定で当てると**発散する**(再投影誤差が天文学的な値になる)。
  トラック長2の不安定な点に引きずられていると見られる。点の足切りが要る
- `mapper --input_path` に既存モデルを渡す方法は COLMAP 4.x では出力が空になる。呼び方の調査が要る

調整させる場合、実寸が保たれる保証が無くなる点にも注意。事後に `model_aligner` で
ARCoreのカメラ位置へ合わせ直す工程が要る可能性がある。

### 6-5. ジオイド高が定数(フェーズ3の宿題)
LASのZに書くのは標高だが、ARCoreが返すのは楕円体高。差がジオイド高で、
`points_to_las.py` は現状 `--geoid` の**定数**を引いているだけ(既定37.0m、名古屋付近の概算)。

数十m四方の点群なら定数で足りるが、広い範囲や高精度が要るときは
国土地理院のジオイドモデル(GSIGEO2011)を読む実装に置き換えること。
これは §6-1(高さの出どころ)とは別問題。あちらはARCoreが返す楕円体高そのものの信頼性の話。

### 6-3. 遡り補正(ループクロージャ)
ARCoreは過去の推定を遡って修正するため、セッション前半と後半で同じ座標値が同じ物理位置を指すとは限らない。フェーズ0では素の値をそのまま記録する方針(対策より先に、ブレの実態を見る)。ローカルと地球座標の2本を突合すればずれた瞬間が見える。本対策はアンカー。

## 7. 実装済みのファイル構成

すべて1つのGameObject(Debug UI)に付け、Inspectorで相互参照を接続:

- `GeospatialStatusDisplay.cs` — 4段ステータス+精度+ローカル座標の画面表示。TextMeshPro(Noto Sans JP, Dynamicアトラス)
- `GeospatialCsvLogger.cs` — 毎フレーム生値のみCSV記録。計算値は書かない。追跡不成立行は空欄(0を書かない)。InvariantCulture必須。60フレームごとflush。NativeShareで共有
- `FrameCapture.cs` — 姿勢付き連番JPEGの撮影+frames.csv。解像度の自動設定と3つのガード(解像度・排他・空き容量)

役目を終えて削除: `VpsCoverageChecker.cs`(カバレッジは絞り込みに使えないと判明) / `CameraConfigLister.cs`(解像度の答えが出て、切替はFrameCaptureが自動化)。必要ならgit履歴から戻せる。

CSV列: unix_ms, elapsed_s, frame, session_state, earth_state, tracking_state, local_p*(3), local_q*(4), lat, lon, alt_ellipsoid, eun_q*(4), acc_h, acc_v, acc_yaw
`elapsed_s` は**アプリ起動からの経過秒**(Time.realtimeSinceStartup)。録画開始からではない。
なお `GeospatialStatusDisplay` の方位角表示だけは計算値(EUNクォータニオン由来)。画面のみでCSVには書かない。

- 取り出し: `adb pull /sdcard/Android/data/jp.seikyo.arpointcloud/files/ .`
- ビルド保存先: プロジェクト直下 `Builds/`
- **バージョン管理: このプロジェクトはまだgit管理下にない**(フェーズ1のStep 0で初期化予定)。root の `ignore.conf` はPlastic SCM用で、gitは読まない。`.gitignore` を別に用意する必要がある
- ビルドの既知の罠: iOS Build Support未導入だとARCore ExtensionsのiOS用エディタスクリプトがCS0234で落ちる(モジュール追加で解決) / Custom Main Gradle TemplateとCustom Gradle Properties Templateの両方が必要(EDM4UのJetifier) / Scenes In Buildにシーン登録がないと"not used by any Scenes"警告

## 8. 検討して外した案(再燃時の参照用)

- 既存写真・動画の後処理だけで完結 → 実寸情報が無く不成立
- 測量基準点(街区基準点等)の再利用 → 探す手間が重い。フェーズ5で1点だけ使う版として残る
- 座標の与え方3段階×記事3本構成 → ARCore方針で置き換え
- 点群×AI(リアルタイムimg2img/3DGS/単眼深度推定) → 保留(提案自体は良い)

## 9. 進め方の流儀(引き継ぐモデルへ)

- 返答は標準語・です/ます調。断定的・説教くさい言い回しは避ける。平易な言葉で、略語は初出時に意味を明示
- 技術は段階的に。一度に大量のコードや数式を出さない。**選択肢を出す前に、判断の土台になる仕組みを先に説明する**
- 本人は仕組みを理解しながら進めたい。結果だけ渡さない。誠実な指摘を歓迎する
- データは生値主義: 現地で判断しない・全部持って帰る・計算はあとから
- 屋内でできることを先に。屋外は1回にまとめる
