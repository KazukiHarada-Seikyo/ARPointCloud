# 現在地（2026-08-12 時点）

新しい会話を始めるときは、この文書 → `ROADMAP.md` → 必要な個別文書、の順に読む。

---

## 一行で

**フェーズ4が閉じた。** 相対（実寸）も絶対（置き場所・向き・高さ）も
数字が出揃った。**実寸は巻尺で外部検証済み: 173cmに対し172.6cm（約1%）。**
残るは密な点群と、OSS・記事の体裁。

---

## 今日到達したこと

### 屋外実測（住宅地）

`Captures/files/rec_20260812_111139`

| 項目 | 値 |
|---|---|
| 枚数 | 2,732枚（CSVと完全一致） |
| 時間 | 314秒 / 経路長 136.1 m / 広がり 対角49.0 m |
| トラッキング断絶 | **なし**（全行 SessionTracking） |
| 方位精度 | 中央値 **4.74度**（5度以下が66.8%） |
| 水平精度 | 中央値 2.36 m ／ 垂直精度 1.33 m |
| 角速度 | 中央値 17.9度/秒（60度超は0.5%） |
| 内部パラメータ | 全行一定（fx=1388.609） |
| 見送り | 5回だけ |

**VPSが成立した状態で撮れている。** 屋内実測（方位16.5度）とは別物。

### 実寸の独立検証 ← **取り下げ（2026-08-12 追記）**

当初「局所座標（加速度計由来）と緯度経度（VPS由来）は出どころが別だから、
一致すれば実寸の裏付けになる。スケール差 +0.10% で実証した」と書いた。
**これは誤りだった。**

区間の前半だけで「局所座標→緯度経度」の変換を決め、後半に外挿すると、
**105秒・34.7m歩いても 2.7 cm しか外れない**（自己申告の水平精度は 2.36 m）。

**緯度経度は独立な測位ではなく、局所座標に固定の変換を掛けたもの**だった。
同じ姿勢を2通りに書いたものを比べていたので、比が 1.0000 になるのは当たり前で、
そこに実寸の情報は入っていない。

`THEORY.md` §11（加速度計が絶対単位を与える）という理屈自体は変わらない。
**「このデータで実証した」と言えないだけ。** 詳細は `PHASE4_ACCURACY.md` §2。

### 疎な点群

`Captures/work_0812_step3/sparse_tri`（911枚 = 3枚に1枚に間引き）

| 項目 | 値 |
|---|---|
| 登録画像 | 911 / 911 |
| 3次元点 | 121,846 |
| 平均トラック長 | **2.37**（低い。68.4%が2視点だけ） |
| 再投影誤差 | 1.598 px |
| 所要 | 46.3分（開発機CPU） |

### LAS出力

`Captures/files/rec_20260812_111139/pointcloud_zone7.las`
121,793点 / 3.0 MB / EPSG:6675 / X=東, Y=北 / ジオイド高 37.75 m

**Unityで色付きで表示できることを確認済み**（別プロジェクトで）。

> 2026-08-12 夜に、基準点の取り方とジオイド高を直して**作り直した**。
> 以前の版とは水平1.96m・鉛直0.75m ずれている。`PHASE4_ACCURACY.md` §5。

---

## 進行中

**5070機で密な点群**（全2,732枚）。手順は `HANDOFF_GPU.md`。

戻ってきたら:

```
python tools/points_to_las.py <fused.ply> Captures/files/rec_20260812_111139 --zone 7 --geoid 37.75
```

数千万点になる見込み。書き出しは流し込み対応済みなのでメモリは大丈夫だが、
LASが数百MB〜1GBになる。最初は `--max-distance 30` で絞ると安全。

---

## フェーズ4でわかったこと（2026-08-12）

詳細は `PHASE4_ACCURACY.md`。道具は2本。

```bash
python tools/analyze_accuracy.py Captures/files/rec_20260812_111139
python tools/check_dem.py        Captures/files/rec_20260812_111139
```

1. **VPSは連続では効かない。** 314秒の撮影で解を組み直したのは**1回だけ**。
   そのとき方位が3.17度回り、高さが0.50m付け替わった。
   段差と段差のあいだは推測航法で、自己申告の精度が毎分0.113度ずつ膨らむ
2. **緯度経度は独立な測位ではない**（上記の取り下げ）
3. **§6-1 に答えが出た。高さは測位由来で、地形データ由来ではなかった。**
   `alt_ellipsoid` は `local_py` に定数を足しただけ（フレーム間の変化 中央値1.10mm）。
   その定数をDEM5A＋ジオイド高37.75mと突き合わせると端末の地上高が2.61mになり、
   地形データから引いているにしては噛み合わない。ずれ **+1.21 m** は
   自己申告の垂直精度1.33mの内側。**1階と上階を測りに行く現地作業は不要になった**
4. **`points_to_las.py` の基準の取り方の不備を修正した。** 緯度経度と局所座標を
   別々に並べ替えた中央値で取っていたため、別のコマを同じ点として結んでいた。
   同じ1コマから取り、最新のVPS解の区間だけを使うようにした。
   → 書き出し直したら点群が **1.96 m 動いた**
5. **ジオイド高の既定 37.0 m はこの場所では 0.75 m 足りない**（正しくは 37.75 m）。
   書き出し前に `check_dem.py` を走らせること

---

## 残っているフェーズ

| | 状態 |
|---|---|
| 0〜3 | 通し済み。LAS の基準の取り方とジオイド高は修正済み |
| 4. 精度測定 | **完了** |
| 5. 公開点群への位置合わせ | 未着手。**遠征不要の可能性**（GSI DEM / PLATEAU） |
| 6. GitHub + Qiita公開 | リポジトリは非公開で存在。記事の前半は `THEORY.md` が使える |

### 精度の現在地

- **絶対（置き場所と向き）**: 水平2.36m / 方位4.74度。
  方位は距離に比例して効き、50m先で約4m、130m先で**約10m**
- **絶対（高さ）**: 測位由来で +1.21 m の偏り。DEMで机上補正できる
- **相対（形と大きさ）**: **巻尺で検証済み（2026-08-13）。約1%の精度で正しい。**
  173cmの巻尺を三角測量して172.6cm。VPSを一切使わない外部検証
- **記事では必ず分けて書く。** 混ぜると「精度10m」と読まれる

### 実寸の裏付け — 済み

黒い塀に巻尺を173cm伸ばして貼り、まわりを歩いて撮影（`rec_20260813_104042`）。
両端を三角測量して **172.6cm**。詳細は `PHASE4_ACCURACY.md` §7。

```bash
python tools/measure_scale.py Captures/rec_20260813_104042 --true-length 1.73
```

> **教訓**: 同じデータでも、貼った直後の45コマ（基線長7cm）だけだと136cmになる。
> 三角測量は基線長で決まる。**基準物のまわりを歩くこと。**
> `FIELD_CHECKLIST.md` に追記済み。

さらに精度を上げるならマーカー、PLATEAUの建物外形（密な点群ができてから）。

---

## 未解決（急がない順）

| 項目 | 内容 |
|---|---|
| 高さが地形を追うか | 撮影地の起伏が0.51mしかなく判定できていない。坂のある場所で1本撮れば片が付く |
| §6-4 姿勢の扱い | `bundle_adjuster` が発散。ゲージ自由度未固定＋トラック長2の点 |
| §6-5 ジオイド高 | 場所ごとに `check_dem.py` で確かめる運用にした。モデル全体を読む実装は未着手 |
| §6-3 遡り補正 | ARCoreが過去の姿勢を修正する件。未着手 |
| レンズ歪み | AR Foundationが係数を出さない。PINHOLEで妥協中 |
| トラック長が低い | 同じ場所を一度しか通っていないため。撮り方の問題 |

---

## 環境の注意（踏んだもの）

**ARPointCloud プロジェクトで `TypeLoadException` が大量に出る**

原因は `Google.Protobuf.dll` の競合。

| 出どころ | 版 | `IBufferMessage` |
|---|---|---|
| ARCore Extensions | **0.0.0.0** | **なし** ← 原因 |
| Unity Burst | 3.30.2.0 | あり |
| Unity 本体 | 3.23.0.0 | あり |

版番号が `0.0.0.0` なので解決順序が起動ごとに揺れる。再起動で直ったり
再発したりするのはこのため。**点群を見るのは別プロジェクトで行う**ことで回避中。

**その他**

- `frames.csv` の古いものにはBOMがある。読むときは `utf-8-sig`（書き出し側は修正済み）
- 日本語コメント入りのPowerShellスクリプトはBOM付きで保存する（5.1はANSIとして読む）
- Unity同梱(NDK)のPythonには `sqlite3` が無い。python.org版を使う
- 開発機のCOLMAPは `nocuda` ビルド。GPUは使えない

---

## 文書の地図

| ファイル | 中身 |
|---|---|
| `STATUS.md` | この文書。現在地 |
| `ROADMAP.md` | ゴール、全体構造、未解決課題 |
| `THEORY.md` | 原理の解説（初学者向け・導出込み） |
| `ARTICLE.md` | 公開用の解説記事。原理＋実測＋ツール解説 |
| `README.md` | OSSの入口 |
| `HANDOFF_APP.md` | 撮影アプリの実装と罠 |
| `VFX_SCAN.md` | スキャン演出（ネオン粒子・スキャンウェーブ）の作りと調整 |
| `HANDOFF_GPU.md` | 5070機での点群生成手順 |
| `PHASE2_COLMAP.md` | COLMAPの手順と実測値 |
| `PHASE4_ACCURACY.md` | 精度測定の結果と、その読み方（実寸検証を含む） |
| `FIELD_CHECKLIST.md` | 現地作業の手順 |
| `DEPARTURE.md` | 出発準備 |
| `UI_SETUP.md` / `UI_SETUP_V2.md` | 画面の作り方 |
