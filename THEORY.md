# 写真から点群ができるまで — 理論編

このプロジェクトが何をしているかを、原理から説明する文書。

**前提知識**: 中学の相似、三角比（sin, cos, tan）、ベクトルと行列の掛け算。
微分は1か所だけ出てくるが、意味は言葉でも説明する。

**読む順番**: 上から順に。§1〜§6でカメラの仕組み、§7〜§9で距離の測り方、
§10以降が実装と誤差の話。§9まで読めば、この手法の核心は分かる。

関連文書: 実装は `HANDOFF_APP.md`、点群生成の手順は
`PHASE2_COLMAP.md`、座標変換は `tools/jgd2011.py` のコメント。

---

## 1. 何が問題なのか

私たちが住んでいるのは3次元の世界で、写真は2次元です。写真を撮るとき、
**奥行きという1次元がまるごと捨てられます**。

点群を作るとは、この捨てられた次元を取り戻すことです。

原理はひとつだけです。

> **同じ点を、違う場所から2回見る。**

2つの視点から伸びる「見通しの線」が交わる場所が、その点のある位置です。
これから出てくる数式は、すべてこの一文の実装に過ぎません。

---

## 2. 針穴写真機 — なぜ像ができるのか

暗い箱の一面に小さな穴をあけ、向かいの面をスクリーンにします。

被写体のある1点からは、**あらゆる方向に光が飛び散っています**。
そのうち穴を通り抜けられるのは、ごく細い1本の光線だけです。
残りは箱の壁に遮られます。

だから「被写体の1点」と「スクリーンの1点」が1対1で対応し、像が結びます。

穴が小さいほど通れる光線が絞られ、像は鮮明になります（そのぶん暗くなる）。
逆に穴が大きいと、1点から複数方向の光が入ってスクリーン上で重なり、
像がぼやけます。

**レンズも役割は同じ**です。1点から広がって出た光を、屈折させて1点に集め直す。
「1点 → 1点」という対応が成り立つ限り、数学の扱いは針穴と変わりません。
以降はすべて針穴で考えます。

---

## 3. 座標を決める

計算のために座標系を置きます。

- **原点 O** は針穴の位置
- **Z軸**（光軸）はカメラが向いている方向。被写体側を正とする
- 被写体上の点を `P = (X, Y, Z)`。カメラの前にあるので `Z > 0`
- **スクリーンは針穴の後ろ**、`Z = −f` の平面に置く

`f` は針穴からスクリーンまでの距離で、**焦点距離**と呼びます。

```
                                            ● P（高さ Y、距離 Z）
                                        ／
                                    ／
                                ／
  ────────────────────────●────────────────────────→ +Z（被写体の方向）
                      ／  O（針穴）
                  ／
              ／
          ●  P′（高さ −fY/Z）
          │
     スクリーン（Z = −f）
```

光線は `P` から `O` を通り、まっすぐ進んでスクリーンの `P′` に当たります。

**`O` を通り抜けるので、上にあったものは下に来ます。**
実際の針穴写真機で像が上下左右ひっくり返るのは、この図のとおりの理由です。

---

## 4. 相似三角形から投影の式を導く

図の中に直角三角形が2つ隠れています。

**大きい三角形**（被写体側）
- 底辺 = `O` から被写体までの光軸方向の距離 = `Z`
- 高さ = `Y`

**小さい三角形**（スクリーン側）
- 底辺 = `O` からスクリーンまでの距離 = `f`
- 高さ = 像の高さの絶対値 = `|y′|`

この2つは**相似**です。根拠は2つ。

1. `O` のところで同じ角をなす（1本の直線がつくる対頂角は等しい）
2. どちらも光軸に対して直角

相似なら対応する辺の比が等しいので、

```
|y′|     Y
──── = ────
  f      Z
```

したがって `|y′| = f·Y / Z`。

符号は図から読み取れます。光線は `O` で軸を横切るので、
**`P′` は `P` と反対側**に来ます。よって

```
y′ = − f·Y / Z
x′ = − f·X / Z
```

**このマイナスが「像が反転している」という物理そのもの**です。

### 別の導き方（確認）

直線 `OP` 上の点は、実数 `t` を使って `t·(X, Y, Z)` と書けます。
スクリーンは `Z = −f` の平面なので、

```
t·Z = −f    →    t = −f / Z
```

代入して

```
x′ = t·X = −f·X / Z
y′ = t·Y = −f·Y / Z
```

相似三角形と同じ答えが出ました。

---

## 5. 虚像面 — マイナスを消す約束事

マイナスを毎回持ち歩くのは面倒です。そこで、
**スクリーンを鏡写しに前へ移した平面**（`Z = +f`）を考えます。

同じ直線が `Z = +f` と交わる点は `t = f/Z` なので、

```
x = + f·X / Z
y = + f·Y / Z
```

大きさは同じで、符号だけ正になりました。この架空の平面を
**虚像面（virtual image plane）** と呼びます。

**この面に光は届いていません。** 実際のスクリーンは後ろにあり、像は反転しています。
虚像面は計算を楽にするための約束事です。

### なぜそれで良いのか

センサーは物理的に反転した像を受けていますが、
**「どの行から読み出すか」の取り決めによって、私たちの手元に届く画像は正立しています**。
反転はハードウェアの段階で打ち消されているので、
私たちが扱う画素は虚像面のものと一致します。

以降は虚像面で話を進めます。ただし、レンズの歪みや
ローリングシャッター（§13）を考えるときは、実物の配置に戻る必要があります。

---

## 6. 長さから画素へ — fx と cx の正体

`x = f·X/Z` の `x` は、まだ**センサー上の長さ**（ミリメートル）です。
画像として扱うには画素番号に直さなければなりません。

センサーの画素が横に `p_x` [mm] 間隔で並んでいるとすると、
長さを画素数に直すには間隔で割ります。

```
画素数 = x / p_x = (f / p_x) · (X / Z)
```

ここに現れる `f / p_x` が **fx** です。

```
fx = f / p_x   [画素]
fy = f / p_y   [画素]
```

**`fx` の単位は画素**です。「焦点距離 1389」はミリメートルではなく、
**1389画素分**という意味です。`fx` と `fy` を別々に持つのは、
画素が正方形でない場合に備えるためです。

もうひとつ、原点がずれています。ここまでの式は光軸を原点にしていましたが、
画像ファイルは**左上の角**から数えます。そのぶんを足します。

```
u = fx · (X / Z) + cx
v = fy · (Y / Z) + cy
```

`(cx, cy)` は光軸が画像を貫く位置で、**主点（principal point）** と呼びます。
理想的には画像の中心ですが、実際には製造誤差でずれます。

**このプロジェクトの実測値**（Pixel 9a、1920×1080）

| 値 | 実測 | 画像中心 |
|---|---|---|
| `cx` | 968.662 | 960 |
| `cy` | 536.514 | 540 |

数画素ずれています。個体ごとに違うので、毎フレーム記録しています。

### 解像度を変えると fx が変わる理由

| 解像度 | fx |
|---|---|
| 640×480 | 463.1 |
| 1920×1080 | 1388.8 |

`463.1 × 3 = 1389.3` で、解像度の倍率と一致します。

理由は式から出ます。物理的な焦点距離 `f` は変わりません。
解像度を3倍にすると同じセンサー面積を3倍細かく刻むので、
**`p_x` が 1/3 になります**。`fx = f / p_x` なので `fx` は3倍。

モデルが実測と合っていることの確認にもなっています。

### v が下向きである理由

画像ファイルは上の行から順に並ぶので、行番号 `v` は**下へ行くほど増えます**。

式 `v = fy·(Y/Z) + cy` が素直に成り立つには、
カメラ座標の **Y軸も下向き**でなければなりません。

OpenCV や COLMAP が「X右・Y下・Z前」を使うのは、この都合です。
恣意的な規約ではなく、**画像の並び順に合わせた結果**です。

Unity は「Y上」なので、両者をつなぐには座標変換が要ります。
このプロジェクトで `diag(1, −1, 1)` を掛けているのは、この1点のためです。

---

## 7. 同次座標 — 割り算を後回しにする

ここまでの式には `X / Z` という**割り算**が入っています。
割り算があると行列で書けず、行列で書けないと、
連立方程式やSVDといった線形代数の道具が使えません。

そこで**割り算をせず、割る前の状態のまま持ち回る**ことにします。

2次元の点 `(u, v)` を、3つ組 `(su, sv, s)` で表します。`s` はゼロでない任意の数。
たとえば点 `(3, 4)` は

```
(3, 4, 1)   (6, 8, 2)   (30, 40, 10)   … すべて同じ点
```

**定数倍の違いを無視する**この表し方を **同次座標（homogeneous coordinates）**
と呼びます。元に戻すには3番目の成分で割ります。

これを使うと投影が行列の掛け算だけで書けます。

```
[su]   [fx  0  cx] [X]
[sv] = [ 0 fy  cy] [Y]
[s ]   [ 0  0   1] [Z]
```

3行目から `s = Z`。1行目は `su = fx·X + cx·Z` なので、`s` で割れば

```
u = (fx·X + cx·Z) / Z = fx·(X/Z) + cx
```

元の式に戻ります。**割り算は消えたのではなく、最後まで先送りされている**だけです。

この3×3行列を **内部パラメータ行列 K** と呼びます。

```
    [fx  0  cx]
K = [ 0 fy  cy]
    [ 0  0   1]
```

### なぜこの道具立てが自然なのか

同次座標では `(sX, sY, sZ)` がすべて同じ点を表します。
`s` を動かすと、これは**原点を通る1本の直線**をなぞります。

一方でカメラが測っているのは、§2で見たとおり**針穴を通る1本の光線**でした。

**同次座標が表すものと、カメラが測るものが、どちらも「原点を通る直線」**なのです。
便利な小技だから使われているのではなく、対象の構造に合っているから使われています。

---

## 8. カメラの位置と向き — 外部パラメータ

ここまでは点がカメラ座標で与えられている前提でした。
実際には点は世界座標にあり、カメラはその中を動きます。

カメラの位置（針穴の位置）を `C`、姿勢を表す回転行列を `R` とすると、

```
X_c = R (X_w − C)
```

読み方は素直で、**まず原点をカメラの位置へ移し**（引き算）、
**次に軸の向きを揃える**（回転）。

展開すると

```
X_c = R·X_w − R·C = R·X_w + t      ただし  t = −R·C
```

> **注意**: この `t` は**カメラの位置ではありません**。
> 位置に回転をかけて符号を反転したものです。間違えやすいところで、
> COLMAP の `images.txt` に入っているのもこの `t` です。
> 位置を取り出したいときは `C = −Rᵀ t`。

`R` は**回転行列**で、次を満たします。

```
Rᵀ R = I        長さと角度を変えない
det(R) = +1     鏡に映さない
```

2つ目が `+1` でなければならない理由は、このプロジェクトで実際に踏みました。
`det = −1` だと**鏡像**になり、点群が裏返ります。

### まとめた形

```
s·x = K [R | t] X_w          （X_w は同次座標で4成分）

P = K [R | t]                （3×4 の投影行列）
```

記録している値が、そのままこの式の部品です。

| `frames.csv` の列 | 式のどこ |
|---|---|
| `fx, fy, cx, cy` | **K** |
| `local_q*` | **R** |
| `local_p*` | **C**（そこから `t = −RC`） |
| JPEG の画素 | **(u, v)** |

---

## 9. 距離はどうやって出るのか

### 9-1. 1枚では出ない

投影の式を逆に辿ります。`K` の逆行列は

```
      [1/fx    0   −cx/fx]
K⁻¹ = [  0   1/fy  −cy/fy]
      [  0     0      1  ]
```

画素に掛けると

```
        [(u − cx) / fx]
K⁻¹ x = [(v − cy) / fy]  ≡  d      （カメラ座標での方向ベクトル）
        [      1      ]
```

そして

```
X_c = λ · d        （λ > 0 は不明）
```

**`λ` が決まりません。** 2倍しても4倍しても同じ画素に落ちます。

**1枚の写真が与えるのは点ではなく光線です。**

### 9-2. 2枚あれば出る

いちばん簡単な配置で導きます。カメラを2台、**同じ向きのまま横に `B` だけ離して**
置きます。この `B` を**基線長（baseline）** と呼びます。

1台目をカメラ座標の原点に置き、点を `P = (X, Y, Z)` とすると、

```
1台目:  u₁ = fx · X / Z + cx
```

2台目は `X` 方向に `B` ずれた位置にあるので、
同じ点は2台目の座標では `(X − B, Y, Z)` に見えます。

```
2台目:  u₂ = fx · (X − B) / Z + cx
```

引き算します。

```
d ≡ u₁ − u₂ = fx·X/Z − fx·(X−B)/Z = fx · B / Z
```

この `d` を**視差（disparity）** と呼びます。
同じ点が2枚の画像で何画素ずれて写るか、という量です。解くと

```
      fx · B
Z = ──────────
        d
```

視差を角度に直すと見通しが良くなります。`θ ≈ d / fx`（微小角）なので

```
Z ≈ B / θ
```

**「基線長 ÷ 見込み角」**。天文学が星までの距離を測るときの式と同じで、
あちらは地球の公転直径を `B` に使います。

---

## 10. 角度だけでは大きさが決まらない — この手法の核心

前節の式をもう一度見ます。

```
Z = fx · B / d
```

**`Z` は `B` に正比例しています。** `B` を2倍にすれば `Z` も2倍になる。

つまり、**視差（＝角度の情報）をいくら精密に測っても、
基線長を知らなければ距離は決まりません。**

物理的にはこう言えます。

> 世界全体を2倍にして、カメラの移動距離も2倍にすると、
> **写真に写る角度はすべて同じになる。**

ミニチュアの模型を近くから撮った写真と、実物の建物を遠くから撮った写真は、
区別がつきません。**角度はスケールに対して不変**なのです。

数学的には、写真だけから作った再構成は**相似変換の7自由度**
（回転3・並進3・スケール1）だけ不定になります。
これを**ゲージ自由度（gauge freedom）** と呼びます。

### どの分野も「既知の長さ」を1本用意している

| 分野 | 基線長をどう得るか |
|---|---|
| 測量の三角測量 | 基線を巻尺や光波測距儀で実測する |
| 天文の年周視差 | 地球の公転直径を使う |
| ステレオカメラ | 2つのレンズの間隔が設計値として既知 |
| **このプロジェクト** | **加速度計が移動距離をメートルで与える** |

---

## 11. 加速度計がスケールを与える — 物理

加速度計の中身は、**ばねに吊るされた微小な錘**です（実際には
MEMSの櫛歯構造ですが、原理は同じ）。

錘がどれだけ変位したかを測り、ばね定数と質量から力を、
力から加速度を出します。したがって出力の単位は **m/s²** です。

> **なぜ静止していても 9.8 を示すのか**
>
> 加速度計が測るのは**比力（specific force）**、つまり
> 「重力以外の力による加速度」です。机の上に置いた加速度計は、
> 重力に逆らって机から支えられています。この支える力が測られるので、
> 静止時に上向き 9.8 m/s² を示します。
>
> 逆に自由落下させると、支える力がなくなるので **0** を示します。

ここが肝心です。

**加速度計の出力は、ばね定数と質量という物理量から導かれます。
これらは工場で校正された絶対的な値です。**
つまり出力の m/s² は、どこかの写真と比べて決めた相対値ではなく、
**メートルという単位系に直結した絶対量**です。

2回積分すれば距離が出ます。

```
速度 v(t) = ∫ a dt        [m/s]
位置 x(t) = ∫ v dt        [m]
```

**これが基線長 `B` です。** そして `Z = fx·B/d` の `B` にメートルが入るので、
`Z` もメートルで出ます。

### VIO — 視覚と慣性を組み合わせる理由

加速度計だけでは長時間もちません。2回積分するので、
わずかな誤差が時間の2乗で膨らみます（**ドリフト**）。
数秒で数メートルずれます。

一方カメラは、形は正確に測れますが大きさが分かりません。

**この2つは弱点が逆**です。

| | 得意 | 不得意 |
|---|---|---|
| カメラ | 形・向き（長時間安定） | 大きさ（原理的に不定） |
| 加速度計 | 大きさ（絶対単位） | 長時間（ドリフト） |

両方を組み合わせるのが **VIO（Visual-Inertial Odometry）** で、
ARCore の姿勢推定はこれです。

**マーカーも基準尺も置かずに実寸が出るのは、
スマホに「長さの単位を持った部品」が入っているから**です。
このプロジェクトが成立している理由が、まさにここにあります。

---

## 12. 対応点をどう見つけるか — エピポーラ幾何

三角測量には「画像Aのこの画素と画像Bのこの画素が同じ物体点」という
**対応**が必要です。総当たりでは組み合わせが多すぎるので、幾何で絞ります。

2つのカメラ座標の関係を `X_c′ = R X_c + t` とします。移項すると

```
X_c′ − t = R X_c
```

つまり3つのベクトル `X_c′`、`t`、`R X_c` は**三角形をなす**ので、
**同一平面上**にあります。この平面を**エピポーラ平面**と呼びます。

同一平面上の3ベクトルは、スカラー三重積がゼロになります。

```
X_c′ · ( t × R X_c ) = 0
```

外積を行列で書き直します。ベクトル `t = (tx, ty, tz)` に対して

```
        [  0  −tz   ty]
[t]ₓ =  [ tz    0  −tx]        （[t]ₓ a = t × a を満たす）
        [−ty   tx    0]
```

これを使うと

```
X_c′ᵀ [t]ₓ R X_c = 0        E ≡ [t]ₓ R  （基本行列 essential matrix）
```

さらに画素で表します。`X_c ∝ K⁻¹ x` なので、

```
x′ᵀ ( K′⁻ᵀ E K⁻¹ ) x = 0     F ≡ K′⁻ᵀ E K⁻¹  （基礎行列 fundamental matrix）
```

**意味**: 画像Aのある画素に対応する画素は、
画像B上の**1本の直線（エピポーラ線）に限られます**。
2次元の探索が1次元に落ちます。

### 実際の流れ

1. **SIFT** などで特徴点を検出する。周囲の輝度勾配のパターンを
   数値の並び（記述子）にして、拡大・回転・明るさの変化に強くする
2. 記述子どうしを比較して候補の対応を作る
3. **RANSAC** で `F` を推定しながら、式を満たさない対応を捨てる

**このプロジェクトの実測値**（286枚）

| | 値 |
|---|---|
| 1画像あたりの特徴点 | 6,168個 |
| 幾何検証を通過したペア | 1,257組 |
| 1組あたりの対応 | 平均973 |

---

## 13. 誤差はどう伝わるか

### 13-1. 奥行き誤差は距離の2乗で悪化する

`Z = fx·B/d` を `d` について微分します。

```
dZ        fx·B
──── = − ──────
dd         d²
```

ここに `d = fx·B/Z` を代入すると `d² = (fx·B)²/Z²` なので、

```
dZ         fx·B · Z²        Z²
──── = − ───────────── = − ──────
dd        (fx·B)²          fx·B
```

したがって、視差の測定誤差 `σ_d` が奥行き誤差 `σ_Z` に化けるとき

```
        Z²
σ_Z = ────── · σ_d
       fx·B
```

**`Z` の2乗に比例し、`B` に反比例します。**

`fx = 1389`、`σ_d = 0.5` 画素として、

| 対象までの距離 | B = 0.3 m | B = 1.0 m |
|---|---|---|
| 3 m | 1.1 cm | 0.3 cm |
| 10 m | 12 cm | 3.6 cm |
| 30 m | **108 cm** | 32 cm |

**撮り方への含意**

- **横に大きく動く。** その場で振り回すと `B ≒ 0` になり、奥行きが発散する
- **近景を主役にする。** 30m先は基線長1mでも30cm級の誤差
- 建物を撮るなら、正面に立ち止まらず**建物に沿って平行移動**する

### 13-2. ローリングシャッター

スマホのセンサーは全画素を同時に露光しません。
上の行から順に読み出し、Pixel 9a では**全体で最大35ms** かかります。

行 `r` の露光時刻は

```
t(r) = t₀ + r·τ
```

つまり**1枚の画像の中で `[R|t]` が変化しています**。
§8までのモデルは「1枚の画像に1つの姿勢」を前提にしていたので、
**この前提が壊れています**。

角速度 `ω` で回しているとき、画像の上端と下端で生じるずれは

```
Δu ≈ fx · ω · T_readout
```

**このプロジェクトの実測角速度で計算すると**

| 角速度 | 上端と下端のずれ |
|---|---|
| 22°/秒（実測の中央値） | 約 19 画素 |
| 100°/秒（実測の最大） | 約 85 画素 |

再投影誤差が1.8画素であることを考えると、**桁がひとつ違います**。
ゆっくり動かす理由は、ぶれを避けるためだけではありません。

`frames.csv` に `angular_speed_deg_s` を記録しているのは、
あとから「この区間は歪んでいる」と判断できるようにするためです。

---

## 14. バンドル調整

三角測量は姿勢を固定して点だけを解きます。
姿勢にも誤差があるので、本来は両方を同時に動かすべきです。

```
min   Σ Σ  ρ( ‖ π(K, Rᵢ, tᵢ, Xⱼ) − xᵢⱼ ‖² )
R,t,X  i j
```

- `π` は投影の関数（§8の `P` を掛けて `s` で割る操作）
- `xᵢⱼ` は実際に観測された画素
- `ρ` は外れ値の影響を抑える関数（Huber損失など）

**再投影誤差**の二乗和を最小にする非線形最小二乗問題で、
Levenberg–Marquardt法などで解きます。これを
**バンドル調整（bundle adjustment）** と呼びます。
「光線の束（bundle）をまとめて調整する」の意味です。

未知数は膨大です（286姿勢×6 + 30,023点×3 ≈ 92,000）。
現実的に解けるのは、**ヤコビ行列が疎**だからです。
ある点は一部の画像にしか写らないので、行列の大部分がゼロになります。
点の変数を先に消去する（Schur補行列）と、姿勢だけの小さな系に落ちます。

### このプロジェクトで発散した理由

実際に当てたところ、再投影誤差が天文学的な値になりました。原因は2つ。

1. **ゲージ自由度を固定していなかった**（§10）。
   全体を平行移動・回転・拡大しても誤差が変わらないので、解が定まらない
2. **トラック長2の点が多すぎた**。
   2視点しか見ていない点は、光線がほぼ平行だと奥行きが極端に不安定になる

対策は「短いトラックの点を落とす」か「姿勢を固定するか強い事前分布を置く」。
`ROADMAP.md` §6-4 の宿題です。

---

## 15. 疎な点群から密な点群へ

ここまでで得られるのは**疎な点群**です。特徴点しかないので、
このプロジェクトの実測では3万点程度でした。

見栄えのする点群にするには、全画素に奥行きを与える必要があります。
COLMAP の `patch_match_stereo` はこう解きます。

1. 各画素にランダムな奥行きと法線を仮に割り当てる
2. その仮説で参照画像へ投影し、周囲の小窓の**輝度の一致度**を測る
   （正規化相互相関 NCC など）
3. 良かった仮説を**隣の画素へ伝播**させる
4. ランダムに微調整して改善を探す
5. 2〜4 を数回繰り返す

**「良い奥行きは空間的に連続している」**という仮定を使って、
探索空間を劇的に減らしています。

画素ごとに独立な計算なので **GPU向き**で、
COLMAP のこの工程が CUDA 必須なのはこのためです。

---

## 16. このプロジェクトの実測値

286枚（33秒、実効8.6fps）で測った値です。

| 方式 | 登録画像 | 3次元点 | 平均トラック長 | 再投影誤差 | 時間 |
|---|---|---|---|---|---|
| ARCoreの姿勢を固定 | **286/286** | 30,023 | 3.61 | 1.80 px | **4.6分** |
| COLMAPの自力推定 | 245/286 | 34,923 | **9.49** | **0.885 px** | 46.8分 |

**読み方**

- **再投影誤差 1.80 px** — 角度に直すと `atan(1.8/1389) ≈ 0.074°`。
  10m先で1.3cmの光線のずれに相当します
- **トラック長 3.61** — 1つの点が平均3.6枚に写っている。
  健全なのは8以上。**視点の多様性が足りない**（同じ場所を別角度から
  撮っていない）ことの表れ
- **登録286/286** — 姿勢を与えているので当然。
  自力推定は41枚を繋げられなかった

**なぜ姿勢を固定すると誤差が大きいのか**

同じ画像なので、ローリングシャッター歪みは両方に等しく乗っています。
違うのは**姿勢を動かせるかどうか**です。

自力推定は姿勢を自由に調整できるので、歪んだ画像に合う姿勢へ寄せて
誤差を吸収できます。固定した側には逃げ場がありません。
**差の一部はこれで説明できる**と見ています。

---

## 17. 全体をつなぐと

```
1枚の写真          →  光線（奥行きが1次元不足）
2視点              →  交点、ただし「形」だけ（§9）
+ 加速度計          →  メートル（§11）          ← この手法の核心
+ VPS（地球座標）   →  地球上の位置と向き
+ 投影計算          →  平面直角座標系 + 標高     ← tools/jgd2011.py
```

---

## 用語集

| 用語 | 意味 |
|---|---|
| **焦点距離** | 針穴からスクリーンまでの距離。`fx` は画素単位に直したもの |
| **主点** | 光軸が画像を貫く位置 `(cx, cy)`。理想は画像中心 |
| **内部パラメータ** | `K`。カメラ自身の性質（焦点距離と主点） |
| **外部パラメータ** | `[R\|t]`。カメラが世界のどこにどの向きで居るか |
| **同次座標** | 定数倍の違いを無視する座標。原点を通る直線を表す |
| **虚像面** | 計算用に前へ移した架空のスクリーン。光は届いていない |
| **基線長** | 2視点の間隔 `B`。実寸を決める唯一の長さ |
| **視差** | 同じ点が2枚の画像でずれる画素数 `d` |
| **ゲージ自由度** | 写真だけでは決まらない相似変換7自由度 |
| **エピポーラ線** | 対応点が乗るはずの直線。探索を1次元に落とす |
| **基本行列 E** | `[t]ₓR`。カメラ座標での対応の拘束 |
| **基礎行列 F** | `K′⁻ᵀEK⁻¹`。画素座標での対応の拘束 |
| **再投影誤差** | 計算した3次元点を投影し直したときの、観測画素とのずれ |
| **バンドル調整** | 姿勢と点を同時に最適化する非線形最小二乗 |
| **トラック長** | 1つの3次元点が何枚の画像に写っているか |
| **VIO** | Visual-Inertial Odometry。カメラと慣性センサーの融合 |
| **ローリングシャッター** | 行ごとに露光時刻がずれる読み出し方式 |

---

## この文書が扱っていないこと

- **レンズ歪み** — 実際のレンズは直線を曲げます。AR Foundation は
  歪み係数を出さないので、このプロジェクトは歪みなし（PINHOLE）として
  扱っています。広角側の直線が曲がる分は誤差として残ります
- **ループクロージャ** — 同じ場所に戻ってきたと気づいて過去の推定を
  遡って直す仕組み。ARCore は内部でこれを行うため、セッション前半と
  後半で同じ座標値が同じ物理位置を指すとは限りません（`ROADMAP.md` §6-3）
- **ジオイド** — 楕円体高から標高への変換。現状は定数で引いています
  （`ROADMAP.md` §6-5）
