# スキャン演出（ネオン粒子とスキャンウェーブ）

最終更新: 2026-08-13
対象: `Assets/Scripts/PointCloudPreview.cs` / `Assets/Shaders/PointCloudPreview.shader`

---

## 何をしたか

特徴点の表示を、単なる半透明の丸から**光る粒**に変えた。加えて、
カメラの位置から**波紋が広がり、通ったところの粒が光る**演出を足した。

| | 前 | 後 |
|---|---|---|
| 粒の見た目 | 半透明の丸 | 芯が加算で光る + にじみ |
| 動き | 現れた瞬間だけ大きく | 上記 + **またたき**（点ごとに位相が違う） |
| 波 | なし | **スキャンウェーブ**（カメラから広がる輪） |
| 誰が動かすか | CPU（0.1秒ごとにメッシュ再構築） | **GPU（時間の関数）** |

**まだ実機で見ていない。** コンパイルとAndroidビルドは通っている
（シェーダも含めてエラーなし）が、**絵としてどう見えるかは未確認**。
APKを入れて見るのが朝の最初の仕事。

---

## すぐ試す

**ビルド済みのAPKがある。** インストールして起動すればそのまま見える。

```bash
adb install -r Builds/arpointcloud_vfx.apk
```

以前の `Builds/arpointcloud.apk`（8/11版）は残してあるので、
見比べたければ入れ替えて確認できる。

自分でビルドし直すときは、Unity を開いて メニュー > ARPointCloud > Android ビルド。
コマンドからなら `-executeMethod BuildAndroid.Build` を渡す（`Assets/Editor/BuildAndroid.cs`）。

> バッチでビルドすると EDM4U（Android Resolver）が
> `Assets/Plugins/Android/*.gradle` と
> `ProjectSettings/AndroidResolverDependencies.xml` を書き換える。
> **中身は自動生成なので git では戻してよい。** 次のビルドでまた入る。

---

## 動かし方

シーンの変更は要らない。`PointCloudPreview` は実行時に描画用オブジェクトと
マテリアルを作るので、**そのままビルドすれば有効**になる。

```
Debug UI の PointCloudPreview コンポーネント
  ネオン
    ネオンの強さ        0.55   ← 屋外で白飛びするなら下げる
    光のにじみ          0.6
  またたき
    またたきの深さ      0.35   ← 0 で止まる
    またたきの速さ      3.0
  スキャンウェーブ
    波を出す            ✓
    波を出す間隔        1.6 秒
    動いたら出す距離    0.7 m  ← 「動かすと波が出る」ぶん
    波の速さ            4.0 m/s
    波の厚み            0.55 m
    波の強さ            1.2
    波が届く距離        12 m
```

Inspector を触ると実行中でもすぐ絵に反映される（`OnValidate`）。

---

## 仕組み

### 演出をGPUに移した

以前はスクリプトが点ごとの色と大きさを毎回計算し、**0.1秒ごとにメッシュを
作り直していた**。点が1万個あると、演出が続くあいだじゅうCPUが4万頂点を
書き直し続ける。

いまは頂点に「生まれた時刻」と「乱数の種」だけを載せ、時間で変わるものは
すべてシェーダで解く。**メッシュを作り直すのは点が増減したときだけ。**

```
uv2.x = 生まれた時刻 (Time.time)
uv2.y = その点の乱数 0〜1  ← またたきの位相をずらす
```

演出を派手にしたのに**CPUの仕事は減っている**。撮影中は端末が熱を持って
1フレームの余裕が無くなるので、ここは効くはず（要実測）。

### ネオンの出し方

全体を加算合成にすると、屋外の明るい映像の上で白飛びする。
そこで**前倍算アルファ**（`Blend One OneMinusSrcAlpha`）にして、

- **にじみ** … 通常の合成のまま（背景を潰さない）
- **芯だけ** … 加算で足す（ここが光って見える）

という配分にした。`ネオンの強さ = 0` にすると、**以前とまったく同じ絵**に戻る。
迷ったらここを0にして比べるとよい。

### またたき

ただの `sin` だと全体がゆっくり明滅するだけで「キラキラ」に見えない。
5乗して山を尖らせ、**ふだんは静かで時々ぱっと光る**形にした。
点ごとに位相をずらしているので、群れがざわざわ光る。

大きさは1より下げていない。粒が消えると「目印の数」を見誤るため。

### スキャンウェーブ

波は「出た場所（xyz）と出た時刻（w）」の4成分だけをマテリアルに渡す。
シェーダ側で半径 = 経過時間 × 速さ として、点までの距離が半径に近いほど
明るくする（ガウス型なので縁が硬くならない）。

- 同時に**2本**重ねられる。1本だと間隔の切れ目で途切れて見える
- `間隔` が来るか、`動いたら出す距離` だけカメラが動くと次の波が出る
- ただし歩いていると「動いたら出す」が何度も成立してしまう。そのまま出すと
  2本の枠を上書きし続けてどの波も途中で消えるので、**間隔の半分は空ける**
- 波が `届く距離` まで行くと薄れて消える

---

## 撮影中は控えめになる

`撮影中は演出を控えめにする` （既定ON）が入っている。録画中は

- 新しい波を出さない（出ている波は最後まで走らせる。途中で消すと不自然）
- またたきとネオンを弱める

`FIELD_CHECKLIST.md` §5 のとおり、炎天下では端末が熱を持って保存が
追いつかなくなる。**見た目より撮れることが優先**なのでこうしてある。
SNS用の画面録画を撮るときは、このチェックを外すと派手なまま撮れる。

---

## やっていないこと

**「空間メッシュ」のスキャンウェーブは入れていない。**

壁や床の面に沿って波が這う演出には、空間の形（メッシュ）が要る。
Pixel 9a は LiDAR 非搭載で、ARCore は面のメッシュを出さない。
出せるのは特徴点と平面検出だけ。

Depth API を使えば深度マップから面を起こせるが、

- 撮影の負荷が増える（発熱は撮影の敵。`FIELD_CHECKLIST.md` §5）
- パッケージ追加とシーン変更が要る
- そもそも本筋（点群の精度）と関係がない

ので、今回は**特徴点の上を波が通る**形にした。当初の案にあった
「特徴点を検知した場所に光の粒子がふわっと残る」は、この形で満たしている。

平面検出（`ARPlaneManager`）はすでに使える状態なので、
「検出した平面の上をグリッドが走る」演出なら追加できる。やるなら次の回。

---

## 確認すること（実機）

- [ ] 粒が光って見えるか。**白飛びしていないか**（屋外の明るい壁で見る）
- [ ] またたきが「うるさい」と感じないか。うるさければ `またたきの深さ` を下げる
- [ ] 歩くと波が出るか。出すぎるなら `動いたら出す距離` を上げる
- [ ] **録画中にfpsが落ちていないか。** 画面の「保存 N枚 / 見送り M枚」を見る。
      見送りが以前より増えていたら `撮影中は演出を控えめにする` が効いているか確認
- [ ] 端末の温度。10分ほど撮って以前と比べる

**見送りが増えるようなら、演出より撮影が優先。** 迷わず戻すこと
（`ネオンの強さ 0` / `またたきの深さ 0` / `波を出す` オフで元の見た目になる）。

---

## 元に戻したいとき

```bash
git checkout Assets/Scripts/PointCloudPreview.cs Assets/Shaders/PointCloudPreview.shader
```

Inspector の値だけで戻すなら、ネオンの強さ0・またたきの深さ0・波を出すオフ。
このとき絵は以前と同じになるが、**CPUの負荷は下がったまま**なので、
戻す場合でもこの版を使うほうがよい。
